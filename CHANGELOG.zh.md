## 0.1.227 - 2026-09-11

- MiniMax H3 多图生成改走 Hybrid：将第一张图锁定为准确 `first_frame`，仅把其余图片传入 `ref_images`，减少所有图片被当作同级 Ref2VA 参考时的重绘、模糊和身份混合。
- 独立图生视频提示词与页面明确首帧和辅助参考图职责；继续使用已验证的 DaSiWa Hybrid pruned、匹配四步 LoRA、原采样、音频和输出参数。
- H3 提交前检查配套 Companion 能力，并升级多图片段缓存签名。已有成片不会自动改变，仅在用户再次发起制作时按新路线生成。

## 0.1.199 - 2026-09-06

- 修复有完整剧本但缺失/部分导演制作稿时无法一键重做：新母版就绪后自动补建缺失稿，保留原剧本、声音、指定参考图与历史成片；网页检查新版 Companion 恢复能力。
- Story 与连续剧共用电影叙事要求，按行动、选择和反应安排节奏；非全片终镜明确可见镜尾交接，区分本镜动作落点与下一镜参考，全片终镜保留余韵。
- 修复“车尾开口”等物理描述被误判成对白；导演局部修稿失败后从锁定视觉信息恢复错误字段，不增加生成后质检、不改变采样设置。

## 0.1.198 - 2026-09-06

- 连续剧“一键重做”会在最新自动角色、场景和道具母版准备完成后，重新生成每镜生图提示词与结构化 H3 导演指令。每次重做使用独立版本号绕过旧导演草稿，清除旧视觉事实和视频分段计划，再按当前流程重做 2×2 四宫格、分镜图、视频提示词、视频及最终拼接。
- 重做视觉时继续锁定已批准剧本合同：镜头数量、剧情、原动作、演员表演、景别、运镜、逐字台词、音色参考和历史成片均不改变。旧分集若没有保存 StoryPlan，会从已批准镜头合同在本地恢复，不调用模型重写剧本。
- 已保留任务号或幂等提交回执的上游图像任务改为可恢复状态，不再显示为连续剧终止失败。队列短暂延迟后继续查询同一付费任务，不重复提交；依赖这些素材的一键重做成片任务不能越过未完成的共享资产。审核拒绝和已确认终止失败仍需明确人工处理；没有增加生成后质检。
- 更新连续剧界面的一键重做说明，使其与真实处理边界一致。本地验证：连续剧 124 项、自动生产 11 项、Story 阶段 67 项、TypeScript 检查及生产构建通过。

## 0.1.197 - 2026-09-06

- 独立图生视频、Story 和连续剧提交给 MiniMax H3 的非台词指令统一改为精简中文，逐字台词仅跟随项目选择的语言；旧英文 H3 提示词在提交前自动转换。保留原动作、表情和运镜，不增加字幕质检或媒体重试循环。
- 人物、产品和场景原始参考图只交给 GPT-Image-2 融合生成分镜；H3 只接收已经融合完成的最终分镜首帧、显式连续接镜帧和音频参考，避免产品原图作为视频插帧出现。
- 连续剧固定道具正名、台词角色遗漏和文本模型不完整输出增加源头自动修正，保留已通过草稿及用户原动作；产品替换会在生图前同步到剧本字段和对象绑定。
- 连续剧新增“一键重做”：保留总纲、分集故事、镜头剧本、逐镜导演描述、音色与历史成片，只重做自动角色/场景/道具参考图、四宫格分镜图、H3 视频和最终拼接；用户上传及角色库指定图不重画。完整本地测试 621 项通过，生产构建通过。

## 0.1.196 - 2026-09-05

- 角色设计支持 MJ/GPT 独立定稿、直接采用原图、角色库成功标记与可选 GPT 延展。原始视觉母版贯通 Story/连续剧；MJ 母版默认交给 GPT-Image-2 生成四格，不重新设计人物。
- 制作风格与拍摄方式在有角色参考时仍按显式选择生效，保留独立风格图与说明；MJ 单独传 sref/sw，修复局部风格图权重丢失，支持草稿恢复与提示词预览。新 Story 默认沿用参考，更换已有素材风格前提示失效范围。
- 原图使用签名直传与原字节持久化，改善 HTML 错误提示；保留原稿动作与产品参考职责，不增加生产质检或自动付费重提。
- 使用已生成宫廷四格实测 2×2 切割、浏览器预处理与单图/多图 H3 接口映射，GPU 提交用模拟替代。既有切割几何、H3 采样、音频和云端工作流不变；同步网页与 Companion。

## 0.1.193 - 2026-09-04

- Story 四宫格提示词明确为两列两行，每个象限都是与整图同比例的完整分镜；移除含糊的“不要模板布局”，排除竖向堆叠、格内再分格和跨格连续场景。
- 无参考图的结构化四宫格请求也使用四宫格提交路径，保留四镜提示词和供应商支持范围内的网格分辨率设置；单张生成与 2×2 切割逻辑不变。
- 同步更新网页、Companion 安装包和本机安装，保留现有项目与图片，不增加图像质检或自动付费重生成。之前布局错误的原图需在更新后手动重新生成对应批次。

## 0.1.192 - 2026-09-04

- Story 编剧前先匹配原稿人物与已选角色卡，按所选人物适配姓名、身份与称谓；保留原动作、剧情、时长及其他台词，复用指定角色卡与已锁定音色。
- 匹配结果贯通编剧与分镜导演，同时保留原稿和人物适配稿；Story 页面展示对应关系及原稿对照。仅新增确实独立的角色，匹配有歧义时提示明确关系，不再静默新增重复人物。
- 成功匹配与编剧结果保存为重试断点；旧普通 Story 剧本在重试编剧时先补人物适配，连续剧已批准合同保持不变。本次同步更新网页、Companion 安装包和本机安装。

## 0.1.191 - 2026-09-04

- Story 导演局部修稿与最终校验共用全剧登记人物、道具名单，不再把已登记的无对白陪衬人物误判为非英文并反复打回。
- 缺失镜头细化时仅补字段，不重写有效分镜；兼容常见 JSON 修稿外壳，将具体拒收原因和候选句反馈给模型，多字段修稿无进展时自动缩小为单字段任务。
- 保留原动作、逐字台词和成功草稿断点；供应商明确拒绝时停止自动修稿，原样返回超限文本不再计为进展。本次修复同步发布网页并更新本地 Companion。

## 0.1.190 - 2026-09-04

- Story 与连续剧自动成片移除额外末镜 ASR 转写门槛及据此付费重生成的循环；视频完成后继续合成，不再受 Fish 转写服务或旧核验结果阻塞。
- 断点重试保留已付费任务编号、原视频地址、缓存键、生成签名和历史诊断；明确提示“已跳过额外核验”，不伪称核验通过。
- 保留原台词、H3 结尾提示词与时长余量，以及导出前全量片段恢复、媒体完整性保护；同步更新网页与本地 Companion。

## 0.1.189 - 2026-09-04

- 角色库、编剧、已保存 Story 剧本与导演阶段贯通登记别名和选角名，统一结构化人物与 H3 说话人 ID；不改逐字台词和原动作，也不强行合并身份不明确的角色。
- 完整提取分行书写的原分镜及动作、运镜信息。保留原稿 A–B–A 对答，不再套用新创对白限制或把同一人物拆成两人；H3 中重复同一镜号的时间段仍作为同一镜的动作阶段。
- 连续剧完整镜头后的原因不明拒绝尾文，可自动补齐一次缺失镜头；原稿、响应停止信息及独占补镜记录持久化，Companion 重启也不会重复提交。供应商明确拒绝仍立即停止，不切换接口或供应商重提。
- 同步更新网页与本地 Companion；发布不增加媒体质检或付费生成。

## 0.1.188 - 2026-09-04

- Story 按登记别名和角色库选角名识别已绑定音色，兼容旧连续剧快照及分段中遗留的音色字段；复用现有声音参考，不猜测角色、不改台词、不重新合成音频。
- 音色暂缺时保留手动分段和已编辑台词，在提交生成前核对当前绑定，不再因此丢弃剪辑方案。
- 修复截断剧本 JSON 被误解析为内层对象、继而误报“缺少 shots 数组”的问题；缺少外层闭合符时本地恢复，缺镜时仅续写剩余镜头并保留完整镜头。明确识别模型拒绝、保留原稿，并停止自动切换接口/供应商及反复修稿。
- 本次同步更新网页和 Companion，保留现有项目数据及媒体素材。

## 0.1.187 - 2026-09-04

- Story 生图步骤的分镜卡片在桌面端改为每行 4 格，平板保留 2 格、手机保留 1 格；图片比例、生成逻辑和已有素材不变。本次仅更新网页排版，无需更新 Companion。

## 0.1.186 - 2026-09-04

- 修复 Story 四宫格创建和单张补图在到达生图供应商前即被拦截的问题：只提交必要分镜字段和图片地址，不再携带保留的原图 base64、无关定妆及历史视频数据。
- 浏览器本地参考图改为独立签名上传，同图跨批次复用上传结果；保持参考图顺序、原提示词、指定素材及可恢复的付费任务编号不变。本地页面沿用现有 multipart 上传通道。
- HTTP 413 和明确的文件大小拒绝不再自动重试，异常超长文本在提交生图前提示。本次仅发布网页，Companion 0.1.184 无需更新。

## 0.1.185 - 2026-09-04

- 清除连续剧界面残留的固定“18个镜头”，操作按钮、进度卡片及当前/历史任务名称统一使用“分镜剧本”，不再继承旧项目的镜数标签；分镜标签页、镜头总数和就绪状态按该集已保存剧本的实际镜数显示。
- 明确参考图按四宫格分批、每集镜数跟随各集剧本。本次仅发布网页界面，不改已有剧本和素材，Companion 0.1.184 无需更新。

## 0.1.184 - 2026-09-04

- Story 分镜参考图从九宫格改为原生 2×2 四宫格；生图提示词、每批数量、服务端裁切、无限画布预览、进度/报错提示及拆分请求全部统一为每批 4 镜。
- 镜头数预设不再按 9/18 递增，改为 4–80 的全部 4 倍数；普通 Story 和自动生成连续剧默认 16 镜。用户粘贴的成型剧本仍严格保留原镜头数，末批不足 4 镜时只取实际镜头。
- 升级不浪费已付费任务：新任务持久化 2×2 规格；升级前中断的旧 3×3 任务会按原九格恢复和裁切，不会误按四宫格拆分或重复提交。

## 0.1.183 - 2026-09-03

- 角色卡、场景参考和自动道具参考均可在各自卡片中单独生成。存在可续查任务编号时继续原任务；只有原提交无法恢复且用户明确点击手动重试时，才授权发起一次新的付费任务。
- 用户指定的人物图片和从角色库选择的人物直接视为生产定稿；自动完稿只补声音或其他空缺，绝不重画这些人物，也不重画用户上传的指定道具。
- 部分素材人工指定后仍可继续批量完稿：已完成和已指定资产保持锁定，其余资产独立补齐；失败提示会直接引导到对应素材的单项恢复按钮。

## 0.1.182 - 2026-09-03

- 重做“分镜 → H3 提示词”转换：每镜按景别、具象动作与表情、运镜路径、收尾状态、逐字台词、声音的顺序线性展开，不再让重复的元合同淹没原剧本镜头。
- 当前对白镜头的提示词缩短约三分之一，同时完整保留已审定 `videoDirection` 的四个字段和全部 `<d>` 逐字台词；角色、图片、音色绑定仍明确，重复的保留、材质、剪辑与语音说明统一合并。
- 从生成源头移除可能反向触发画面文字的字幕词汇枚举，改为正向要求“无图文包装的摄影原片、对白仅存在于音轨”。仍不增加 OCR、字幕质检或去字幕修复流程；提示词合同升级为 `h3-v39`。

## 0.1.181 - 2026-09-03

- 按 H3 官方六段式重新组织生成提示词：画面导演说明保持英文，只有逐字中文台词留在 `<d>[Chinese]`；无字幕、系列风格、定点修复和成片结尾规则统一前置到原剧本镜头正文之前，不再追加在最后一句台词之后。
- 从源头抑制音色参考的校准语音泄漏到片头：所有绑定音频明确仅参考音色、不得复制原始信号；移除每句台词的硬起声时间戳，并把台词前声床限定为非人声现场环境。
- H3 提示词完整保留已审定剧本的动作、可见细节、运镜路径、收尾位置、台词顺序和原文。提示词合同升级为 `h3-v38`，旧合同视频缓存会重新生成，且不增加字幕或音频质检流程。

## 0.1.180 - 2026-09-03

- 移除成片保存阶段写死的90–150秒限制；连续剧现在忠实采用剧本及实际导出时间线，无论短于或长于两分钟都可正常交付。
- 保留必要的交付安全校验：成片仍须具备可读取的正数时长、视频轨、音轨、项目画幅，并已由本地导出器确认时间线完整、没有截断。
- 首页不再宣传固定“18镜 / 约2分钟”，改为明确镜数与时长跟随项目剧本。

## 0.1.179 - 2026-09-03

- 手动新增全剧固定道具即视为剧情需求：自动加入分集故事反向重写队列，保留历史成片，并强制该道具至少在一个分集及其详细镜头中承担具体、可见的行动或线索，不再只作为资产入库。
- 成型原稿在获授权插入新道具时仍受保护：模型只能追加一小段可见道具互动，原动作和生图提示词全文、逐字台词、镜头边界与顺序必须完整保留。
- 移除连续剧生产预检中最后一处写死的 18 镜要求；Story 规划、交付审计、队列名称和生产合同统一按项目实际 `shotCount` 验收，当前 9 镜成稿也可直接通过。

## 0.1.178 - 2026-09-03

- 新建连续剧的前台与领域校验均支持 1 集；粘贴已成型分镜稿时仍自动识别为单集，并采用原稿镜头数。
- 权威成稿模式不再为了匹配固定道具正名而改写用户原文，已验证的 `objectIds` 直接承担资产绑定；“黑色面膜”与“黑灰色纱布面膜”这类自然说法差异不再让保留草稿陷入反复修稿，普通生成剧本仍保留严格的正文落地校验。
- 后续重扫资产时保留成稿的显式道具绑定；制作队列按项目实际镜头数显示任务名称，不再固定写“18镜剧本”。

## 0.1.177 - 2026-09-03

- 自动识别带连续镜号的已成型拍摄剧本，将其视为权威成稿而不是故事创意：保留原镜头数、动作、景别、运镜、氛围、AI 生图提示词、逐字台词与顺序，不再扩写额外集数或新剧情。
- 仅当原时长客观说不完逐字台词时延长该镜；角色、场景、道具仍按最终资产正名映射，文本模型改写原动作或台词时拒绝落库。旧项目下次进入队列重试时自动撤销此前的填充式扩写，同时保留已准备资产和历史交付。
- 中文 `<d>` 台词明确只作为音轨与口型数据，并把“画面不得显示对白文字”的约束放在全部镜头描述之后；H3 提示词合同升级为 `h3-v37`，受影响的旧视频缓存不会复用，且不增加字幕 OCR、质检或去字幕耗时。

## 0.1.176 - 2026-09-03

- 移除导演主提示词中“`videoDirection` 同时写中文和英文”的冲突；示例、输出字段和提交前自检统一要求英文画面导演说明，仅允许已登记的中文角色与物体正名原样保留。
- 断点局部修稿新增明确的正名白名单，并在写入断点前执行语义校验；中文引号词、对白概念和其他非正名文本不再被误判为修稿成功。
- 单次可定点修复最多六个污染字段，保留其他有效分镜字段和原有重试上限，减少重复请求。

## 0.1.175 - 2026-09-03

- 在进入分镜制作前，按最终角色与固定道具档案反向校正每集18镜剧本：安全补回本镜发声角色，一次收集并定点修复全部未落入画面的道具引用；镜头数偏差可归并或拆分，但不能增删、改写或调序原台词。
- 手动「写18镜」发现公共资产未完成时，先自动排入角色、场景与道具定稿；一键成片则在进入 Story 前全自动完成同一闭环。
- 每集保存并展示自动反向修稿的时间、数量与具体内容，不再静默修改草稿。

## 0.1.174 - 2026-09-03

- 连续剧角色成功保存后立即显示行内成功提示，并为与角色库中最新形象、描述和音色一致的角色卡常驻显示“已入库”标签；角色卡或音色变化后自动取消标签，提示重新更新入库。

## 0.1.173 - 2026-09-03

- 角色卡支持逐个加入制作队列，已完成的连续剧角色可连同角色卡和可复用音色信息一键保存到当前浏览器角色库。
- 全剧固定道具区分“总纲识别后自动生成参考图”和“用户上传指定参考图”：自动道具在资产定稿时生成独立连续性参考，品牌商品或已有实物图保持为不可重画的用户来源。
- 修复旧项目因自动识别道具缺图而无法点击资产定稿的问题；旧数据按编号兼容识别来源，生图继续使用可恢复的任务断点。

## 0.1.172 - 2026-09-03

- 导演字段的增量修复提示词统一输出英文画面描述，与 H3 校验规则一致，避免中文项目在草稿污染或超长后反复修复失败。

## 0.1.171 - 2026-09-03

- 中文项目的 H3 画面导演说明恢复为全英文，仅在 `<d>[Chinese]` 中保留逐字中文台词；无字幕约束收敛为一次简短正向规则，并将旧项目中文画面元数据安全降级为英文描述，降低烧录字幕概率。
- 一键成片取消分镜角色一致性、视频重复人物和烧录字幕的大模型质检与自动返修，继续依赖锁定参考和提示词合同，不再因质检阻塞或重生成。
- 无尾帧依赖的视频片段按最多两段并行，明确接续上一段尾帧的片段仍保持顺序；保留任务断点并兼容此前已付费片段，新提示词缓存合同升级为 `h3-v36`。

## 0.1.144

- 修复跨平台 Companion 发布检查，使生图安全测试在全新 GitHub 构建环境中正确解析共享 TypeScript 模块。

## 0.1.143

- 新增一张全系列风格参考，仅把文化主题、色调、色温、镜头语言与光线贯穿角色卡、场景、分镜和视频提示词；更换参考时归档旧视觉生成，保留剧本、音色及历史成片。
- GPT Image 2 实拍提示词改为简洁的电影现场摄影描述，明确加入“超强真实感”，围绕剧情动作与可见摄影依据表达，移除容易加重 CG 感的渲染/VFX 术语堆叠。
- 强化无人值守生产：持久化生图提交与恢复信息、保留上游拒绝记录、仅在人工语义复核后补充成年角色说明重试、有限中断恢复，并在导出前恢复完整18镜视频。
- 对成片片段核验有证据的重复角色身体及末镜对白，仅修复受影响片段，并完整保留已付费任务历史。
- Midjourney V8.2 支持内容参考与独立 `sref`/`sw` 风格控制，采用最小供应商参数，并按单镜逐张生成分镜。
- 失败制作任务可单独删除，不影响剧本、素材、断点及已完成成片。

## 0.1.142

- 仅整部成片最后一镜规划1秒无对白/旁白的自然收尾，按完整剧本识别末镜，不给中间镜头或每个生成片段添加此规则。
- 预览及网页/本机导出仅保护整片结尾不被智能提速压短；调整顺序后保护随末镜移动。保留原画面和声音，不强制静音、不补黑帧或定格；已生成的配音不会自动重新排期。

## 0.1.141

- 四个视觉字段共享720字符总预算，单字段略超写作参考值但总量合格时不再拦截完整描述；保留最终H3的7000字符上限和视觉/对白校验。

## 0.1.140

- 局部摄影修稿保留已经改好的字段和缩短后的中间稿，不因剩余超限退回长稿；网络失败采用独立有限重试，不占用内容修稿机会。

## 0.1.139

- 电影运镜按起始机位、触发、方向/幅度/速度与可见落点细写，区分电影调度、纪实跟拍与固定监控。
- 解除全程锁死构图与运镜的冲突，保留人物、材质、场景及首尾帧约束。
- 刷新实际提示词会结合可用分镜图重新设计摄影；普通预览仍复用有效稿，保留已生成素材。
- 新稿自动修正模糊微调和固定/移动矛盾；超长优先只改必要字段，保留有效画面细节。

## 0.1.138

- 单张分镜完成后保存供应商图片到素材存储，避免项目仅依赖供应商地址。保存失败保留付费任务编号，从同一生成结果重试，不重复生图。

## 0.1.137

- 参考核验与单帧物种核验冲突时，回到原角色参考复核可见差异，避免把定稿拟人化设计当成角色替换而反复补图。
- 真实的缺角/串角仍失败；复核不可用仍记为未知，不伪装通过。

## 0.1.136

- 供应商最近仍返回处理中时，继续查询原付费任务，不消耗内容纠错次数。
- 明确的限流/临时拥堵和TLS建立前连接失败采用独立、最多6次退避重试；不扩大不确定付费提交超时、余额或鉴权错误的重试。

## 0.1.135

- 角色参考核验和单帧核验统一遮挡规则：不再把美人鱼被衣服/画幅遮住的鱼尾误判为人腿，仍拒绝实际可见的错误解剖结构。
- 自动切换逐镜生成时显示当前镜头编号，避免继续显示九宫格阶段。

## 0.1.134

- 修复GPT单镜提示词被旧4,000字符限制截断：完整保留角色对应关系、参考图用途和输出约束，避免角色漂移与额外文字。

## 0.1.133

- GPT实拍角色先生成单张定妆主图，再扩展辅助多视图卡；制作始终绑定主图，防止多视图再次引入CG媒介或错误解剖。
- 主图与角色卡分别保存任务断点；加入独立摄影质感检查，失败或未知如实记录，不伪装通过。
- 重选角色或修改外观会清理旧主图；保留既有已定稿资产。

## 0.1.132

- GPT实拍角色卡改为定妆摄影参考，锁定身份、服装和物种，不继承旧参考图的CG媒介；明确的动漫/CG/参考风格不变。
- GPT场景采用单幅勘景照片，单镜和九宫格统一身份与摄影风格的职责。
- 角色补图中断后先恢复缺图，再执行视觉核验；已有素材不覆盖。

## 0.1.131

- MJ 分镜改为逐镜生成，移除九宫格及自动转用其他生图模型；其他模型保持原流程。
- 有参考图的单镜使用实测通过的 V8.2 编辑请求，完整保留角色身份、参考图映射、服装与镜头动作；固定原始参考，避免逐镜累积漂移。
- 每镜核验人物与单画面构图，先检查已付费候选，再有限次局部重试；未知核验结果保留警告，不伪装通过。
- 单镜任务号立即同步到断点，保留已有合格图片及视频。

## 0.1.130

- 连续剧新增1:1方形画幅，从项目保存、Story制作到成片交付保持一致；修复方形成片被误判为横屏规格的问题。
- 保持既有项目画幅和已生成素材不变。

## 0.1.129

- 所有集共用导演字段局部修稿：自动重写超长、非英文或混入声音指令的运镜字段，不截断文本，不重写已通过的分镜和台词。
- 提示词刷新复用相同纠错规则，临时请求失败保留原稿；整季台词超时改用独立局部修稿上下文。
- 保持原有限额、有限次重试与断点缓存，网页和 Companion 同步使用完整运镜简报。

## 0.1.128

- 修稿同步视频分段的独立对白合同，避免分镜已改而 H3 仍生成旧台词。
- 开始制作前自动修复旧断点的对白不一致，仅清理受影响片段，保留其他已付费视频及所有图片、音色。

## 0.1.127

- 自动检测相邻镜头对白错配到不同角色，按本镜职责和动作局部修稿；保留图片与固定音色，仅重制受影响的视频片段。
- 修稿缓存绑定源剧本，避免复用过期台词；拒绝只替换虚词而保留错误角色含义的修稿。

# 更新日志

## 0.1.126 - 2026-08-31

- 同源分镜读取只返回 H3 所需的最多1600像素、1.6MB以内预处理图，避免4K PNG占满网页响应；不改变项目原图或已付费任务编号。

## 0.1.125 - 2026-08-31

- H3 读取 APIMart 分镜时通过同源服务端通道，修复图片 CDN 无 CORS 导致的 Failed to fetch；保持原图和任务标识，不重新生成。
- 素材读取限定生成域名、禁止重定向，设置45秒超时与25MB流式上限。

## 0.1.124 - 2026-08-31

- 刷新恢复只补缺失九宫格单元，不再把已完成的单张补图误判为旧裁图迁移；保留完整画面和已付费视频任务。

## 0.1.123 - 2026-08-31

- 补图前同步更新生成状态，完成后同步保存结果，避免 React 延迟更新误跳过补图。
- 分开处理类人近景与非人动物体态核验，避免裁切误判；空的额外角色结果不触发重制。

## 0.1.122 - 2026-08-31

### 修复

- 连续剧在视频生成前自动核验分镜角色；发现缺人、串角或重复人物时只修复对应画面，保留已完成片段，最多两次补图。
- 核验服务拒绝或不可用时明确保留待复核提示，不冒充通过，不绕过供应商拒绝。
- 纯声音角色不强制入画，动物与幻想角色保留物种特征；红色色彩描述不误触伤害词修订。

## 0.1.121 - 2026-08-31

### 修复

- 九宫格提示词先为所有镜头、完整人物清单及参考图映射分配容量，避免整段截断丢失第8、9镜；供应商边界保留已分配的完整合同。
- 超出九宫格容量时自动走逐镜生成与断点恢复，不静默遗漏人物或镜头。

## 0.1.120 - 2026-08-31

### 修复

- 连续剧分集按开场、目标、冲突、选择、回报、钩子校验；独立 Story 保留七个叙事里程碑要求，不再混用两种叙事合同。
- 进入导演前预检18镜、台词、固定音色和音频时长；旧版连续剧规划从保存的定稿安全重建。
- 已定稿同镜 A-B-A 保留有序发声及有意重复的台词，跨镜仍保留分段限制。

## 0.1.119 - 2026-08-31

### 修复

- 已定稿连续剧直接以结构化18镜合同进入导演，保留同镜 A-B-A 对话、逐字台词、声音和时长，不再重复编剧触发冲突。普通 Story 创作流程保留。
- 自动选声候选不足时继续搜索 Fish 公共音色，明确来源、试读校验后固定，不再仅因为授权小库耗尽就要求手选。公共展示不等于平台授权，项目需符合音色使用许可。

## 0.1.118 - 2026-08-31

### 修复

- 有闪回、肖像、照片或录像形象的角色必须制作角色卡；生成总纲时只有明确全程无可见形象的角色才接受“仅声音”分类。
- 角色编辑新增出镜类型，修改保留已固定音色和历史成片；分集编剧上下文携带出镜类型。
- 纯旁白占位明确显示无需角色卡，完成状态显示“声音已定稿”。

## 0.1.117 - 2026-08-30

### 修复

- 连续剧角色可直接浏览、按名称搜索 Fish 公共库、平台授权库和自有工作区，播放现有试听样本并固定选择；搜索框默认留空，不以剧情角色名搜索同名音色。
- 公共及工作区音色选择前明确确认使用权，不将公共可见冒充平台授权；用户指定音色在定稿时校验目标语言试读。
- 自动候选不足仅标记该角色待选声，继续准备其他角色和场景，最后汇总待选清单；账号、网络和存储故障仍立即停止。
- 固定声音时保留角色形象及历史成片，清除旧的声音候选、试读和错误，不随机复用他人音色。

## 0.1.116 - 2026-08-30

### 修复

- Story 与连续剧的视频声音提示逐镜保留环境底声和动作拟音，不再把多镜头声音合并后截取前四项，避免后续场景声音丢失或错配。
- 明确对白之下保留可听的环境底声，延续同场景声场并尊重明确静默；编剧不再默认把所有声源写成微弱、轻柔或无实际声源的视觉动作。
- 不修改已有视频、不自动重制或添加外部声音轨；实际混音仍取决于原生视频模型的生成结果。

## 0.1.115 - 2026-08-30

### 修复

- 视频提交后的即时断点保存使用最新剧本规划，避免异步执行闭包短暂覆盖为旧版；保留已提交的视频任务编号。
- 恢复及认领任务清除历史错误，制作中不再显示上次“已暂停”的红色错误信息。

## 0.1.114 - 2026-08-30

### 修复

- 连续剧在进入导演前，按定稿镜号直接绑定结构化动作、人物、时长与逐字台词，不再从带角色标签的展示文本重新解析对白。
- 对镜头调序、未登记角色和缺失音色先行报错；保留已完成的编剧规划与导演批次断点。
- 连续剧分镜逐镜绑定已定稿场景图，九宫格按镜号标注各场景参考，不再默认全片使用第一张场景。

## 0.1.113 - 2026-08-30

### 修复

- 自动制作恢复时复用源稿、镜数和音色一致的已完成编剧规划，导演失败不再从头编剧；完成编剧即保存断点。
- 编剧与导演按批次保留原稿，恢复时先校验并复用；只修失败批次。摄影描述同时反馈全部超限字段，保持硬限制并缩短目标长度。
- 普通断点保存不再覆盖队列当前的具体制作阶段。

## 0.1.112 - 2026-08-30

### 修复

- 一次列出全部超时对白与逐句字数预算，定向缩短模型台词，保留18镜、镜头时长、说话人、画面与动作；补写后重新校验，不靠拉长成片或丢弃对白过关。
- 对白修稿支持原稿保存、断点复用与非法补丁保护。

## 0.1.111 - 2026-08-30

### 修复

- 分集缺少故事等必填文字时，一次列出所有缺失字段与精确路径，只补写缺失内容，保留原稿其余字段；补齐后仍严格校验人物、场景、伏笔及用户修订。
- 非法修稿补丁不会覆盖可恢复原稿；有效缓存可在重启后复用，避免重新购买同一份编剧结果。

## 0.1.110 - 2026-08-30

### 修复

- 已生成的图片超过图床上传限制时，复用同一原图进行 WebP 编码后补上传；先尝试无损编码，再使用高质量编码，保留原尺寸、方向与透明度，不重新请求生成。
- 大图恢复限制下载体积与解码像素数，不将网络或账户错误误判为图片过大；无法安全压缩时保留任务编号并明确报错。

## 0.1.109 - 2026-08-30

### 新增

- 制作队列的当前与历史失败任务支持删除；仅移除任务记录和所存配置，保留剧本、制作断点、素材和成片。
- 服务端在持久化事务中校验项目归属与失败状态，拒绝删除正在执行、排队、暂停或已完成的任务；旧版 Companion 明确提示更新。

### 验证

- 25 项连续剧回归测试、类型检查、隔离 API 冒烟测试与页面删除/刷新验证通过；验证其他任务、项目与成片不受影响。

## 0.1.108 - 2026-08-30

### 修复

- 修正把音色原始语言当作TTS硬限制的问题：原语言优先，不足时从平台授权库与自有工作区选取跨语言候选。
- 跨语言试音必须通过实际语言识别和标准文本匹配，再锁定音色；音频和验证结果落盘，ASR故障重试只补校验。
- 当前任务与历史记录分开显示，补充更新时间；服务端禁止历史任务重复重试和同阶段重复排队。

### 验证

- 268项测试与类型检查通过。He Jin真实英语试读约9秒，ASR识别en且标准文本匹配100%，音频已缓存。未启动图片、视频或整季编剧。

## 0.1.107 - 2026-08-30

### 修复

- Companion通过网站临时签名保存媒体，不再依赖安装包内的Cloudinary凭据；媒体直传，避免网站大文件请求上限。
- 试读先检查保存通道，音频落盘后上传；上传失败、重启和并发请求复用同一结果，不因存储、额度或网络错误反复更换音色合成。
- 分集逐集校验、逐集保存；提供明确伏笔清单，保留失败原稿并定向修稿，不再整批重写或靠硬塞ID通过检查。
- 自动选声同时检索自有工作区与平台授权库，区分真实来源，并筛选语言、性别、占用与下架状态。

### 验证

- 260项测试与类型检查通过，覆盖上传失败后的重启恢复、并发去重及p4漏项修稿；生成服务使用受控响应，不宣称已完成真实声音表演验收。

## 0.1.106 - 2026-08-30

### 新增

- 连续剧标题旁新增「删除连续剧」，二次确认后移入左栏回收站；可恢复故事、角色场景、任务断点和历史成片，不影响原角色库或普通Story。
- 执行中任务须先暂停并完成断点保存；删除会暂停待执行队列，回收站里的剧不能编剧、修改、重试或继续制作。恢复后保持暂停，不会自动开始付费生成。
- 删除采用可恢复方式，素材文件保留，不释放磁盘空间。删除和恢复要求Companion v0.1.106或更新版本。

### 验证

- 253项测试、类型检查、隔离删除/恢复API及浏览器确认、取消、恢复流程通过；验证旧成片文件保留、恢复后可下载、过期写入和误重试被阻止。未删除真实用户项目或触发模型生成。

## 0.1.105 - 2026-08-30

### 修复

- 总纲与角色、场景清单就绪后，可以独立定稿公共资产，不再被未完成或待修订的整季分集锁住；镜头剧本与成片仍要求完整有效的分集。页面明确显示按钮前置条件和上次分集编剧失败原因。
- 连续剧选角保留完整角色卡、原库形象与同名历史图片地址。完整卡加载失败时自动显示可用参考图，选用时只保存实际加载成功的图片，不将失败地址带入制作，不重新生图或改写原角色库。
- 全部候选图片无法加载时显示说明、支持刷新检查并阻止误选。独立定稿需要 Companion v0.1.105 或更新版本。

### 验证

- 251项测试、类型检查、隔离定稿API与浏览器图片回退/选角保存验证通过；未触发真实生成，保留失败编剧任务与已有故事。

## 0.1.104 - 2026-08-30

### 修复

- 连续剧导航改为「故事总纲 → 角色与场景 → 分集故事 → 制作队列 → 成片」，打开及切换连续剧时默认进入故事总纲。
- 仅调整网页导航，不改变剧本、资产、制作队列或已有项目数据；继续兼容 Companion v0.1.103，无需重新安装。

## 0.1.103 - 2026-08-30

### 新增

- 连续剧主角、配角均可从角色设计库和 Story 历史角色中选角，支持搜索与预览；保留剧中名字、身份、关系和故事内容。
- 完整角色卡或已有形象直接复用，不重新生图；继承库内音色，仅补缺少的试音。所选演员未存音色时，保留剧中单独指定的音色，其余自动匹配。
- 换角仅使受影响剧集的制作素材失效，保留剧本和旧成片；重复选择同一个演员不会丢弃已完成定稿。
- 历史本地图片只在选用时保存到现有图床。读取角色库不改写原记录，角色库仍按浏览器和站点隔离。

### 验证

- 249项测试、类型检查、隔离选角API检查，以及浏览器搜索、选择、保存流程通过；未调用付费生成。角色库选角需要 Companion v0.1.103 或更新版本。

## 0.1.102 - 2026-08-30

### 新增

- 独立「连续剧制片」模块 `/series`：从全剧总纲展开分集故事，规划局部回报、伏笔与结尾钩子，生成可编辑的每集18镜、约两分钟剧本。
- 全剧共享角色与场景定稿；未指定音色时搜索 Fish 授权音色，检查合成可用性并锁定，全剧复用。
- 本地持久制作队列，支持暂停、断点恢复与重复任务拦截；新版 Companion 独立执行器可在关闭网页后继续工作。
- 单集或批量成片、媒体检查、逐集下载及历史版本保留。修改故事会标记受影响的后续稿件，不删除旧成片。

### 验证与使用要求

- 仓库244项测试、Companion生产构建、隔离队列/API、媒体检查、分段下载、桌面及窄屏检查通过；未调用付费生成。
- 连续剧本地保存和后台执行需要 Companion v0.1.102 或更新版本；普通 Story 项目与连续剧任务相互隔离。
- 真实声音与视频效果仍需用样片验证；自动选声目前依据资料匹配和合成可用性，不包含演技评价。

## 0.1.101 - 2026-08-30

### 修复

- 自动成片刷新图片和任务状态时保留片段级权威台词，与手动生成使用同一套输入，避免将已完成的手动片段误判为需要再次付费生成。
- 自动继续和项目恢复均识别手动生成的缓存；旧分镜级台词缓存仅在编译后的片段台词完全一致时复用，修改图片或片段台词仍会正常失效。

## 0.1.100 - 2026-08-30

### 修复

- 视频片段默认从当前分镜图开始；同场/同地点 ID、空 ID 或旧版叙事连续性标记不再自动把上一段尾帧替换为首帧。
- 片段面板增加明确的首帧来源选择。尾帧接续必须是相邻已完成片段、非空且一致的场/地点、没有换场转场，禁止跳过未完成片段借用更早的视频。
- 片段缩略图始终显示原始分镜；生成视频在独立区域展示。旧版自动接续成片会标记为需重生成并阻止混入合并，但不删除历史付费资源。
- 输入未变、原本就从自身分镜开始的旧片段继续复用；错误自动接续片段需重跑，新生成签名升级为 h3-v34。
- ComfyUI 的提示词预览和实际提交统一由同一个 Companion 编译。最低版本提升为 v0.1.100，并同步此前仅部署在线上的观察式拍摄提示词改进。

### 验证

- 新增默认首帧、空元数据、换场、多分镜前段、断档、旧片段兼容、预览分离与 Companion 提示词跨域回归测试。

## 0.1.99 - 2026-08-30

### 调整

- 重构拍摄提示词的核心逻辑，把人物行为线和摄影机观察线拆开：人物始终专注于场景中的事情，摄影机只能在动作已经发生后再跟随和修正。
- 电视直播抓拍、观察纪录、路人手机、新闻长焦、家庭录像、监控机位和电影叙事，统一改用“原任务→触发→延迟反应→不完整调整→回到原状态”的具体因果动作，不再只堆叠“自然、真实”等抽象形容词。
- 单张生图只选择动作链中一个物理成立的瞬间，不再把完整过程压进一张图；九宫格按不同动作阶段展开，并确保九个镜头完整保留。
- H3 时间段表演加入自然停顿、未完成动作、摄影机滞后修正、低活动时刻与动作余韵，避免人物为了填满时长而持续表演。
- 无声画面会清理“自言自语、像要说话、嘴唇似乎说了半句、听见声音”等容易被 H3 误读为台词的描述，减少剧本外人声。
- H3 长提示词压缩时优先保留镜头顺序、动作因果、摄影机反应和权威台词约束。

### 验证

- 仓库 29 组回归测试全部通过，覆盖拍摄预设、九宫格完整性、H3 提示词、各生图供应商、故事阶段、物体一致性和字幕安全。
- Next.js 生产编译、Lint/类型检查、静态页面生成和差异检查全部通过。
- 本次为线上提示词生成更新，无需更新 Companion。

## 0.1.98 - 2026-08-29

### 调整

- 新增 GPT-Image-2 专用提示词编译器，真人摄影任务只使用一种明确的物理拍摄系统，不再混用手机、微单、电影机和直播长焦等互相冲突的语言；动画、3D、定格等非真人风格继续保持原生媒介。
- `gpt-image-2-official` 现在明确请求高保真质量档；普通 GPT-Image-2 保持更快的标准通道。
- GPT-Image-2、Seedream、Nano Banana、Grok、Midjourney 和本地生图都会把参考产品/物体视为不可重新设计的固定对象：锁定轮廓、比例、零件布局、材质、表面处理、颜色、标识和物理尺度，只允许改变视角、摆放、光线及合理的活动部件。
- 产品自身的标签和 Logo 会按参考图原位置保留，同时继续禁止字幕、说明文字、水印、UI 和模型擅自新增的文字。
- 当模型参考图数量达到上限时，当前镜头使用的物体参考图会优先进入请求，不再被角色或场景图静默挤掉；Midjourney 对含产品的故事镜头和九宫格也会适度提高参考图权重。
- 九宫格提示词会明确标注角色、环境和物体参考职责，并在模型长度预算内完整保留九个不同镜头。

### 验证

- 仓库全部回归测试通过，新增覆盖 GPT-Image-2、Midjourney、参考物体一致性、九宫格完整性和供应商请求参数。
- Next.js 生产编译、Lint/类型检查、静态页面生成和差异检查全部通过。
- 本次为纯线上网页更新，无需更新 Companion。

## 0.1.97 - 2026-08-29

### 调整

- 单图生视频与首尾帧生视频默认切换到已实测通过的 DaSiWa 四步 H3 方案：现有 pruned FL2VA INT8 主模型、已校验的 DaSiWa 四步加速 LoRA、INT8 文本编码器、Sage Attention、4/12/3 采样，并关闭近似缓存。
- 浏览器里仍保存旧“均衡八步”默认值的用户会迁移一次到 DaSiWa 四步；八步方案和旧远端工作流仍保留为可选回退。
- 多参考图 Ref2VA 在完成独立质量测试前继续使用原远端工作流，不被这次单图测试越权替换。
- Story 视频生成最低需要 Companion v0.1.97，避免线上界面把新方案交给旧版本地服务执行。

### 验证

- DaSiWa LoRA 安装到 4090D 节点前，已按作者公布的 SHA-256 完整校验。
- Nana“上海街头逛街 / 电视直播抓拍”8 秒单图 I2VA 付费实测耗时 156.8 秒，输出 1280×736 / 24fps，并带完整 8 秒 AAC 音轨。
- 逐帧检查通过人物身份、服装、肢体、前景遮挡、首帧与时间稳定性；低电平环境声没有产生可信的 Whisper 人声识别。
- H3 回归测试与 Next.js 生产构建通过。

## 0.1.96 - 2026-08-29

### 修复

- 修复 fal H3 Max 任务已经生成完成，但 AID 在查询状态/结果时收到 HTTP 405、无法回传成片的问题。
- 任务提交仍使用 `minimax/h3-max/image-to-video`，状态与结果轮询则按 fal 官方客户端规则归一到父应用 `minimax/h3-max`。
- 已保存的 `fal:request_id` 会直接通过正确路径恢复，不会重新购买或提交一次生成。

### 验证

- 使用本次已完成的生产 Request ID 复现了旧路径的 HTTP 405，并确认 fal 成片资源仍可正常访问。
- 新增状态/结果精确 URL 回归；fal、自动生产、视频片段相关测试和 Next.js 生产构建通过。

## 0.1.95 - 2026-08-29

### 新增

- Story 自动生产和独立图生视频页面新增 `fal · MiniMax H3 Max` 视频通道，由模型一次原生生成画面、对白、环境声和拟音。
- 支持 5–15 秒、480P/768P、可选尾帧、异步队列恢复，以及 Cloudinary 回传失败时使用 fal 原始结果。
- Settings 新增独立的 fal API Key 密码输入框；Key 与现有浏览器本地设置一起保存，只在提交或查询 fal 任务时发送给 AID 服务端代理。
- 新增可选的全项目固定 Seed，用于复现性实验；界面明确说明它只是采样随机种子，不是 Voice ID，也不能保证跨片段音色一致。

### 调整

- Prompt Expansion 默认关闭，避免 fal 静默扩写 AID 的逐字台词。
- fal 没有音频参考时，只允许英文声音气质描述出现在对白标签之外；中文角色备注不会进入 H3 导演说明。
- 视频缓存签名加入视频供应商和 fal Seed，切换配置后不会错误复用旧成片。

### 验证

- 新增 fal 队列请求体、默认策略、Seed、状态轮询和结果返回回归测试。
- 216 项全量回归测试和 Next.js 生产构建全部通过。
- 本次为纯线上接入，无需更新 Companion。

## 0.1.94 - 2026-08-29

- 新增项目级“一键成片实际制作耗时”：从自动生产启动计时，到最终合并导出完成停止。
- 计时状态随项目持久化，刷新、导入导出和断点继续后不会丢失；主动暂停期间不计入制作耗时。
- 视频片段编辑页实时显示制作中/暂停/完成耗时，导出页保留最终总耗时。
- 自动生产在不可恢复错误时会暂停计时，避免故障后继续虚增；重新继续时从断点恢复累计。

## 0.1.93 - 2026-08-29

### 新增

- Story 的“全片制作风格”区域新增项目级拍摄方式，可选择电视直播抓拍、观察纪录、路人手机、新闻长焦、家庭录像、监控机位、棚拍广告、电影叙事或跟随参考。
- “电视直播抓拍”会统一约束远距离长焦观察、非摆拍体态、前景行人遮挡、偏构图、轻微切边、运动模糊、直播压缩与真实皮肤质感。
- Story 批量 Excel 支持可选的“拍摄方式 / Capture Mode”列；未填写时使用电影叙事。

### 修复

- 拍摄方式现在会实际进入导演分镜、MJ V8.2 最终请求、Nano Banana 九宫格/单镜提示词和 Companion 内最终 H3 提示词，不再出现“界面已选择、视频端未收到”。
- MJ 角色参考图只提供人物身份；最终 prompt 与负面词会忽略角色卡排版、角色名字和文字标题，减少角色卡文字被带入故事画面。
- 切换拍摄方式会使对应场景图、分镜图、视频和 H3 缓存失效，避免沿用旧模式生成结果。
- H3 提示词缓存合同升级为 `h3-v33`，视频通道所需最低 Companion 版本提升到 v0.1.93。

### 验证

- Nana“上海街头逛街 / 电视直播抓拍”真实链路通过：MJ V8.2 + `votj2t8` 角色首帧、8 秒 H3 视频、无字幕、无台词、无音乐；Whisper 转写为空。
- 新增拍摄方式跨导演、MJ、九宫格、H3、项目持久化和缓存失效回归；相关测试、TypeScript 校验与生产构建通过。

## 0.1.92 - 2026-08-28

### 新增

- 成片导出新增“分镜感知智能节奏”。默认“智能标准”会保护情绪特写和对白，同时分别加速常规叙事、建立镜头、细节镜头、蒙太奇和动作镜头。
- 视频编辑器新增“原速、电影、智能标准、紧凑”四档节奏，并在导出前显示全片实际平均倍率。
- 多分镜 H3 片段会保留逐分镜节奏时间窗，同一生成片段内的 2–3 个镜头可分别变速后无缝重组，不改写剧本台词。

### 调整

- 预览播放、裁剪、时间线时长、定位、Companion 任务恢复和 FFmpeg 导出统一使用同一份素材—成片时间映射。
- 画面与同期对白使用保音高的 `atempo` 同步变速；防爆音尾部淡出改在变速完成后执行。本机导出最低需要 Companion v0.1.92。

### 验证

- 新增节奏分类、成片时长、裁剪、双向时间换算和原速回退测试。
- 本机 FFmpeg 回归覆盖混合倍率、有声与无声音素材、画幅统一、任务恢复和变速后成片时长缩短。
- 195 项全量回归测试、TypeScript 校验与生产构建全部通过。

## 0.1.91 - 2026-08-28

### 修复

- 修复片段先被估算为 6 秒、最终对白时间轴因对应人物稍晚出场而实际需要 6.2 秒，导致生成前被拒绝的问题。
- 视频时长现在直接使用最终对白编译器逐秒反算：6.2 秒会自动提升为 7 秒，最多补到 H3 的 15 秒上限，不改写或加速播放权威台词。
- 服务端会再次计算最低可播放时长，旧项目或过期页面即使提交了偏短时长，也会在生成前自动补足。
- 如果对白在 15 秒内仍无法配合对应分镜出场，自动镜头编组会提前拆开片段；手动组合则返回明确的拆分提示，不再提交必然失败的任务。
- H3 片段缓存合同升级为 `h3-v32`，Story 视频通道所需最低 Companion 版本提升到 v0.1.91，避免旧时长算法继续处理新版片段。

### 验证

- 新增 6.0/6.2 秒边界回归，以及延迟起声超过 15 秒时自动拆段的回归用例。
- 191 项全量回归测试、TypeScript 校验与生产构建全部通过。

## 0.1.90 - 2026-08-28

### 新增

- I2VA/FL2VA 新增正式 `balanced8` 均衡加速方案：pruned FL2VA INT8 主模型、NVFP4 文本编码器、官方 768P 八步 Turbo LoRA、Sage Attention、8 次 NFE 与 6/3 视频/音频 Shift 作为不可拆分的整套配置应用。
- Settings 会显示当前 FL2VA 方案，并保留明确的旧版回退选项；独立的多参考 Ref2VA 路径不受影响。

### 修复

- 修复旧远端工作流加载四步 LoRA 却执行八步采样的配置错配。API prompt 在提交前会锁定并验证 UNET → Sage → LoRA → Sampler 模型链，旧工作流组件或缓存参数不再能静默混用不兼容的权重与调度。
- 连接检查会根据 ComfyUI 实时节点定义验证模型和 LoRA 文件名，并逐个工作流返回实际采用的加速配置。
- Story 视频通道所需最低 Companion 版本提升到 v0.1.90，避免线上界面把正式 FL2VA 任务交给尚未锁定均衡方案的旧版 Companion。

### 验证

- 官方 1.96GB 768P 八步 LoRA 已安装至生产 ComfyUI 主机，并通过发布方 SHA-256 校验。
- 使用同一提示词、参考图和随机种子的 6.584 秒 A/B 实片测试通过：人物身份、环境、手部、服装和美人鱼尾细节稳定；两版均为 960×960、24fps H.264 画面，并含同步 AAC 音轨，响度基本一致。
- 强制完整重采样的热运行耗时 147.9 秒，旧方案同素材为 151.7 秒；切换模型后的冷运行耗时为 168–174 秒。
- 通过已安装的 v0.1.90 Companion 生产接口复跑最近一条 14 秒首尾帧 Hybrid 成片：实际输出 14.375 秒、960×960、24fps H.264 + AAC，完整模型链与 8/6/3 参数均命中；耗时 460.0 秒，旧方案同一成片为 483.6 秒，约缩短 4.9%。
- 189 项全量回归测试、TypeScript 校验、ComfyUI 实时模型/方案校验和生产构建全部通过。

## 0.1.89 - 2026-08-28

### 调整

- 多分镜 H3 提示词改为按整个片段分配演员调度预算：主表演者保留更多细节，配角仍保留必要的可见表情与即时反应。
- 合并重复的时间段、视线和参考保持说明，同时不改写逐字 `<d>` 台词、每镜动作、图片绑定或电影剪辑语法。
- H3 提示词缓存合同升级为 `h3-v31`；Story 生成所需最低 Companion 版本提升到 v0.1.89，避免线上规划把新提示词合同提交给旧版本地引擎。

### 修复

- 修复合并两个至四个分镜后，详细演员调度反复展开，导致 H3 提示词超过 7,000 字符并在提交前失败的问题。
- 当镜头已经包含详细演员表演卡时，不再重复附加通用表情说明，避免提示词膨胀和同一表演出现两套相互竞争的指令。
- 提示词确实无法压缩时，会明确指出是逐字台词或演员任务需要拆段，不再反复重试同一个超长请求。

### 验证

- 四分镜连续性压力用例包含每镜三名演员、超长动作/表情/视线/呼吸调度、两条逐字台词和音色参考，最终提示词为 6,952 字符，并完整保留所有镜头与台词。
- 181 项发布回归测试全部通过，覆盖故事规划、电影化分段、H3 提示词与音频、声音选角、图片生成、任务恢复、自动成片和项目隔离；线上网站与 Companion 生产构建同时通过。

## 0.1.88 - 2026-08-28

### 新增

- 剧本生成新增三阶段动态构思面板，真实显示“故事结构构思、电影镜头组织、交付检查”，并持续展示当前处理内容、等待时长与目标镜头数。
- 长篇剧本规划改为先锁定全片故事脊柱，再按场次生成有界镜头地图，避免一次返回超大纲要，同时保持全片统一的因果权威。

### 调整

- 自动视频分段只合并具有明确剪辑关系的镜头，包括正反打、动作—反应、主镜头—推进覆盖、细节插入、蒙太奇与匹配桥接；长镜头、长对白、关键表演特写和终场英雄画面继续使用保真锁定的独立镜头。
- 多镜头 H3 提示词会逐次说明真实切点，并保持 180 度轴线、视线、银幕方向、动作相位、空间地理和有目的的景别递进。
- 多镜参考图被明确视为彼此独立的实拍机位，不再作为插值目标；禁止画面渐变、身份融合、重复覆盖和无动机软转场。
- 自动分段方案新增版本化电影剪辑合同，会重建 v0.1.87 遗留的全单镜边界；用户手动组合的片段保持不变。
- 剧本与电影化视频分段所需最低 Companion 版本统一提升到 v0.1.88，避免网站把新规划和 H3 合同提交给旧版本地引擎。

### 修复

- 恢复 v0.1.87 将所有分镜完全拆开后丢失的电影化多镜头组织，同时继续为关键镜头保留 I2VA/FL2VA 一致性。
- 先生成精简故事脊柱，再以最多六镜一组请求精确镜头地图，降低长纲要被截断和只返回局部 JSON 的概率。

### 验证

- 170 项发布回归测试全部通过，覆盖故事规划、分阶段剧本交付、电影化分段、H3 提示词与音频、声音选角、图片生成、项目隔离、任务恢复和自动成片；TypeScript 校验与生产构建同时通过。

## 0.1.87 - 2026-08-27

### 调整

- Story 自动成片默认改为一张分镜图生成一个 H3 片段，让每张分镜图成为真实运动起点，不再与多张构图参考图一起被模型平均重画。
- 单分镜现在把图片直接接入 H3 的 `first_frame` 并使用 I2VA；存在音色参考时通过 Hybrid 保留锁定首帧，连续镜头仍使用 FL2VA。
- H3 提示词新增参考优先级、人物身份/服装/环境不可变项、允许变化项、真实材质细节，以及从每镜表演信息生成的三阶段动作时间表。
- Story 视频通道所需最低 Companion 版本提升到 v0.1.87，避免旧版 Ref2VA-only Companion 静默处理新版保真请求。

### 修复

- 修正单图工作流曾把所有分镜都接入 `ref_images` 并提交为 Ref2VA 的问题；该错误会让人物面孔、服装和场景在动作开始前就被重新绘制。
- 出场人物绑定现在同时读取表演卡与权威台词，避免镜头中已描述的演员没有绑定到参考主体。
- 同一人物的连续台词合并为一个 H3 发声块时，不再删除、替换或补写剧本标点。
- 新版保真优先自动分段启用后，旧项目已经付费生成的多镜片段仍可正常恢复。

### 验证

- 90 项 H3 音频、H3 提示词、视频分段、故事交付与分阶段剧本测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.86 - 2026-08-27

### 新增

- 只输入一句故事构想，也可以智能扩写成包含人物欲望、冲突、转折、关键选择与完整结局的电影级剧本。
- 每个镜头中的出场角色都会获得独立表演调度，包括走位、主手势、微表情、视线、呼吸、即时反应与潜台词。
- 分镜审核页新增动作调度、逐字台词与演员表演卡，可在生成图片或视频前直接检查每镜的可执行细节。

### 调整

- 长耗时的故事规划与导演分镜接口改为持续发送保活事件，并在完成时返回结构化结果，不再等待一次性的大段 JSON 响应。
- 人物性别与年龄分组会继续传递给编剧阶段，提升人物塑造与声音选角的一致性。
- 剧本生成所需最低 Companion 版本提升到 v0.1.86，确保网站与本地剧本引擎使用同一套数据结构和请求逻辑。

### 修复

- 浏览器无法连接本地 Companion 时，会直接提示开启 `pandais.beauty` 的“本地网络”访问权限，不再静默回退到最终返回 HTML 网关错误的托管接口。
- Companion 状态检查会自动重试；任务中途断开时也会返回可由 Story 页面安全展示的结构化错误。

### 验证

- 生产构建、分阶段剧本、故事引擎、故事交付、H3 提示词、流式响应与 Companion 路由测试均通过。

## 0.1.85 - 2026-08-26

### 调整

- 按 MiniMax 官方 `h3-prompt-writing` 规范重新设计视频提示词：Ref2VA 使用六段英文结构，首尾帧使用三段英文结构；只有 `<d>` 内保留原语言台词。
- 台词只保留起声时间与一个 `<d>` 标签，不再发送结束时间、时长填充、“只说一次”或口型、闭嘴等收尾指令。
- 中文镜头素材在视频提示词阶段转换成简洁的英文物理动作，减少 H3 把导演说明误读为额外台词的风险。
- H3 提示词缓存合同升级为 `h3-v28`，Story 视频所需的最低 Companion 版本提升到 v0.1.85。

### 修复

- 最终提交边界会清除旧提示词中的“说完最后一个字就闭嘴”、`final word`、`mouth closes`、`stop speaking` 等控制语，同时保持 `<d>` 内的准确台词不变。
- 已保存的旧 JSON、中文控制合同或过期提示词不再直接提交，会自动使用新版提示词重新生成。
- 修正首尾帧提示词的参考图对齐：`Picture 1` 表示开头，`Picture 2` 表示结尾。

### 验证

- H3 提示词、参考音频、无字幕、视频分段、故事交付测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.84 - 2026-08-26

### 调整

- 中文 H3 提示词从源头改用短句和大白话描述人物、物体、动作、表情、景别、相机、连续性和声音，不再使用合同式术语。
- 镜头说明改成“熊猫博士拿着面膜”“相机全程固定”“人物不要突然换位置”这类现场可以直接执行的句子。
- 台词继续只指定起声时间，并直白写成：到点开始一次、只说标签中的台词、最后一个字说完就闭嘴。
- H3 提示词缓存合同升级为 `h3-v27`，不再复用由上一版复杂措辞生成的片段。
- Story 视频所需的最低 Companion 版本提升到 v0.1.84，网站不再允许旧版本地生成器静默提交另一套提示词。

### 修复

- 所有自动生成的中文导演说明都不再包含“可见”；ComfyUI 最终提交边界也会清理已保存或缓存提示词中的该措辞，同时保持 `<d>` 内准确台词不变。
- “总结面膜价值”一类抽象叙述不再作为镜头动作发送，避免被 H3 当成额外台词。
- “物体合同、视线轴、画面侧、动作走位、透视关系、决定性后果”等术语全部改成摄像机能直接拍到的大白话。

### 验证

- H3 提示词、参考音频、故事交付和无字幕回归测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.83 - 2026-08-26

### 调整

- H3 Ref2VA 提示词改为官方六段自然语言格式；实际提交内容不再包含自定义 `timeline_json`、schema、合同或伪 API 字段。
- 每个镜头改写为官方 `[Shot N]` 段落，只包含参考画面、构图、可见人物和物体、物理动作、表情、相机与空间连续性。
- 台词直接写入对应镜头，只指定一个起声时间和一个官方 `<d>[Language] 准确台词</d>` 标签；不再发送结束时间或台词时长目标。
- 首尾帧生成改用官方三字段基础格式，无配乐片段使用 `non_diegetic_music: N/A`。
- H3 提示词缓存合同升级为 `h3-v26`，不再复用由旧 JSON 提示词生成的片段。

### 修复

- 删除可能被 H3 误读成台词的抽象剧情结果、观众理解、关系意义和 JSON 字段标签。
- 禁止字幕、标题、标志、水印、界面和任何可读文字的要求继续以明确自然语言保留。
- 参考音频现在把音色绑定到 H3 局部说话人别名，同时明确排除样本原词、停顿和时序。

### 验证

- H3 提示词、参考音频、无字幕和视频分段回归测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.82 - 2026-08-26

### 调整

- 台词时间继续只指定起声：`first_word_at` 仅包含纯时间戳，并使用简短不可朗读枚举规定起声前静音和到点只启动一次准确台词。
- 提示词结构升级为 `aid_h3_timeline_v8`，缓存合同升级为 `h3-v25`，不再复用仍含结果叙述的旧片段。

### 修复

- `可见后果:` 和 `Visible result:` 现在是硬截断边界；任何视觉说明发送给 H3 前，都会删除该标签及其后全部内容。
- 不再把抽象剧本 consequence 自动拼接到模型可见 action，包括本地 `speech` 为空的片段参考分镜。
- 所有 action 统一只保留摄像机能直接记录的身体、表情、道具、位置和物理状态变化；剧本生成提示也会把功效、价值、依据和观众理解留在剧本元数据中。

### 验证

- 74 项 H3 提示词、视频分段和分阶段剧本测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.81 - 2026-08-26

### 调整

- H3 镜头合同现在只包含可直接拍到的表情、动作、台词事件引用、构图、相机和空间关系。
- 台词正文只存在于 `dialogue_events[].spoken_once`；镜头合同只引用 `D1`/`D2`，不再复制可朗读的台词文字。
- 提示词结构升级为 `aid_h3_timeline_v7`，缓存合同升级为 `h3-v24`，不再复用由剧情化视觉合同生成的旧片段。

### 修复

- `stateBefore`、`stateAfter` 或 `editBridge` 中关于观众理解、接受、疑问和期待的关系叙述不再泄漏到 H3 载荷并被误读成台词。
- 从模型可见的镜头合同中删除 `blocking_relation`、`motion_physics`、重复的逐镜声音、剧情式转场和冗长连续性说明。
- 剧本生成阶段的关系状态现在只允许填写可直接看到的人物距离、朝向、视线轴和银幕侧。

### 验证

- 93 项 H3 提示词、参考音频、视频分段和分阶段剧本测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.80 - 2026-08-26

### 调整

- H3 对白事件不再包含 `duration_policy`、`natural_from_exact_text` 或其他开放式“自然时长”指令。
- 提示词结构升级为 `aid_h3_timeline_v6`，缓存合同升级为 `h3-v23`，不再复用按自然时长策略生成的旧结果。

### 修复

- 对白标签闭合边界现在是绝对停声点：立即终止全部人声、闭合嘴部，之后只允许场景底噪。
- 明确禁止在准确标签文字之后续说、即兴、补音、加字或为了填满剩余片长继续发声。
- 特殊对白闭合 token 只存在于 `spoken_once` 内，避免在可读导演说明中重复控制 token。

### 验证

- 55 项 H3 提示词、无字幕、视频分段和自动成片测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.79 - 2026-08-26

### 调整

- H3 对白时间现在只由 `dialogue_events[].first_word_at` 表达；画面时间轴区间只保留镜头合同和视觉动作阶段。
- 提示词结构升级为 `aid_h3_timeline_v5`，缓存合同升级为 `h3-v22`，不再复用含隐式台词时间窗的旧片段。

### 修复

- 从视觉时间区间中移除 `dialogue`、`voice` 和口型表情状态，避免 H3 将 `00:00.800-00:10.300` 之类画面区间理解为必须持续发声的时长。
- 台词只在精确起声点开始一次，完全按照标签文字的自然时长说完，随后恢复场景底噪，不存在显式或隐式结束边界。
- 对白镜头的语义说明因防误读而被过滤后，会补入克制且可观察的表演动作，不再生成空白 `action` 合同。

### 验证

- 55 项 H3 提示词、无字幕、视频分段和自动成片测试，以及 TypeScript 校验和生产构建均通过。

## 0.1.78 - 2026-08-26

### 调整

- H3 对白事件现在只发送精确的 `first_word_at` 起声时间；台词结束估算只留在内部做容量校验，不再发送给模型。
- H3 时间轴合同升级为 `aid_h3_timeline_v4`，缓存合同升级为 `h3-v21`，避免继续复用受旧结束时间规则影响的片段。

### 修复

- 移除 `window`、`final_word_complete_by`、`CONTINUE_TO_FINAL_WORD` 及结束边界控制，避免 H3 为填满预计时长而拖长台词、补音或自行加字。
- 台词从指定首字时间自然起声，以自然语速准确说一次，完成标签中的最后一个字后立即停声闭嘴，并恢复场景底噪，不再对齐目标结束时间。
- 画面分镜边界不再终止或重启正在说的台词，只能在明确的 `first_word_at` 安排新的对白起声。

### 验证

- H3 提示词、无字幕、视频分段、自动成片、TypeScript 与生产构建检查均通过。

## 0.1.77 - 2026-08-26

### 调整

- H3 对白进行中的时间轴现在只保留 `D1_START_ONCE`、`D1_CONTINUE_NO_RESTART` 等不可朗读控制码；可读的动作、表情和转场说明移入独立视觉镜头合同。
- 第五步提示词编辑器现在展示真正发送给 ComfyUI 的完整提示词。空白时点击编辑会先自动生成；用户明确保存的修改会按原文提交，不再被嵌套成“补充画面要求”。
- ComfyUI 创建任务后会返回经过最终引用清理的实际提示词，项目存档与编辑器因此和供应端载荷完全一致。

### 修复

- 阻止 H3 在同人物连续长台词中朗读 `timeline[].boundary`、动作阶段、表情或转场导演说明。
- 中文视频转场发送前会过滤 `centralDramaticQuestion`、`causal trigger`、`audienceInference` 等编剧脚手架字段。
- SSH 隧道或状态查询临时失败时保留已提交的 ComfyUI 任务号，退避后重新挂接，不再误标失败或重复购买生成任务。
- 提示词预览现在会计入连续性首帧造成的参考图编号偏移，与最终 H3 请求的图片顺序一致。

### 验证

- H3 提示词、无字幕、视频分段、自动成片、TypeScript 与生产构建检查均通过。

## 0.1.76 - 2026-08-26

### 调整

- 台词正式归属于视频片段计划：先为片段锁定一份有序台词表，分镜只提供画面节拍、动作、表情、景别、走位和时间参考，不再各自拥有一套待拼接台词。
- 同一片段允许 A→B、A→B→C 等多人依次对话；每个人物仍只有一个连续发声块，A→B→A 因为需要同一音色二次起声，会自动拆分或明确拒绝。
- 同一人物相邻的生成台词会改写为一段自然连贯的长台词，不再保留容易让 H3 误判成多次起声的句末停顿。
- 片段时长和自动分组改为用一条连续对白列车与画面时间线共同估算，不再为每个分镜重复叠加一份台词时长。

### 修复

- 多分镜合成一个视频时，不会再唤回旧的分镜级台词，也不会提交带静默空洞的同人物两段台词。
- 旧版 v1 片段计划会在载入时自动迁移为 v2，并尽量保留有效的手动分段边界；修改人物音色时也会同步写入权威的片段台词。
- 提示词预览、H3 正式提交、缓存签名、台词校验和第五步声音白名单现在统一读取同一份片段级台词。

### 验证

- 全部 183 项自动化测试、TypeScript 校验和生产构建均通过。

## 0.1.75 - 2026-08-26

### 调整

- MiniMax H3 提示词升级为 `aid_h3_timeline_v3` 合同：最高优先级的音频事件锁和逐字对白事件置于最前，后续依次描述参考音频规则、视觉合同、镜头合同，以及一条按秒连续覆盖的时间轴。
- 时间轴的每个区间都会明确绑定起止秒数、所属镜头、动作阶段、表情、对白状态、声音状态、景别机位、人物关系与边界行为；完整片段从 00:00 到结束不再存在隐含空档。
- 剧本创作、剧本改编、台词编排和视频分段统一限制：同一人物在一个 H3 片段中只能有一段连续台词。相邻的同人物短句会合并成一段更自然的长台词，A-B-A 这类说话人回返则显式拒绝或自动拆到下一片段。
- 中文生成台词每个说话段目标为 24–48 个汉字，英文为 12–28 个单词，让 H3 连续完成一次完整表达，不再反复短起声、停顿和重新起声。

### 修复

- 移除同一人物连续短句之间的静默空隙，避免 H3 在空隙中自行续说、泄漏参考音频原词或生成嗯声等填充音。
- 不同说话人的交接缩短为 0.12 秒，同时保证每位说话人的首字不会早于其来源镜头，并保留片段开头与结尾的干净余量。
- 逐字对白只在顶层声明一次，时间轴仅通过事件 ID 引用，避免动作、表情、机位或声音说明变成第二套互相竞争的发声指令。

### 验证

- 全部 181 项自动化测试、TypeScript 校验和生产构建均通过。

## 0.1.74 - 2026-08-26

### 调整

- MiniMax H3 继续保留官方 Context-IR 外层字段及顺序，片段内部的密集镜头时间线改为严格可解析的 JSON；镜头区间、动作、相机、转场、同步声音和对白不再挤在一长串容易歧义的自然语言里。
- 逐字台词现在只存在于 `shots[].dialogue_events[].spoken_once`；首字起声时间和完整尾字结束时间使用独立字段，JSON 键名与非对白字段均明确为不可朗读的控制数据。
- 无文字画面要求改为结构化布尔合同，分别禁止字幕、说明文字、标题、对话气泡、标志、水印、UI 和任何可读文字。

### 修复

- 多镜头后段不再把 `d` 台词标签紧贴在冗长导演说明旁边，降低 H3 把动作或声音指令误读成额外台词的概率。
- 密集 JSON 提示词仍能保持在 H3 的 7000 字符上限内，不会删掉逐字对白、对白时间、因果动作、运镜意图或镜头转场。

### 验证

- 全部 177 项自动化测试、TypeScript 校验和生产构建均通过。

## 0.1.73 - 2026-08-26

### 调整

- MiniMax H3 对白改为唯一且权威的起止时间窗，并放在画面动作说明之前；首字和尾字都有明确边界，时间窗外保持闭嘴且不得出现可辨识人声。
- 有台词镜头会隔离动作、可见后果和听者状态中重复台词含义的说明，避免 H3 在正式 `d` 标签台词前先朗读语义复述。
- 视频提示词预览现在使用当前实际音色绑定，与 Companion 最终提交的提示词保持一致。

### 修复

- 首尾帧 H3 提示词会保留 `<Picture 2>`，并显式绑定两张参考图中的角色与音色，不再把尾帧替换成中英文混杂的占位文字。
- 对白镜头清理抽象产品价值和观众认知说明时，会保留 `Dr. Pan` 这类带句点的人名及其物理动作。

### 验证

- 全部 177 项自动化测试、TypeScript 校验和生产构建均通过。

## 0.1.72 - 2026-08-26

### 修复

- MiniMax H3 原生台词条件节点现在始终携带工作流必填的 `audio_denoise_strength` 字段，并固定为完全原生生成值 `1`，修复单次 H3 迁移后 `MiniMaxH3AudioConditioningT8` 缺少字段导致的任务创建失败。
- 新增回归断言，确保单次原生工作流保留 `audio_denoise_strength: 1`，同时不再带入已经移除的 `drive_audio` 路径。

### 验证

- 全部 175 项自动化测试、TypeScript 校验和生产构建均通过。

## 0.1.71 - 2026-08-25

### 调整

- Story 有台词片段改为一次 MiniMax H3 原生音画生成；成片直接使用主 H3 AV 解码音轨，不再先独立合成台词驱动音轨、后期重混，也不再为环境声重复运行第二次完整 H3。
- Fish Audio 继续只作为音色参考；每句剧本台词只在官方 H3 `d` 对话标签中出现一次，Story 内部说话人 ID 不再暴露给 H3。
- 中英文提示词都加入明确的对白白名单：对话标签外均为无声导演指令，章节名、时间码、动作、声音说明和参考音频原词都不得被念出。

### 修复

- 移除互相竞争的独立语音、drive audio、人声分离和母带链路，避免双层人声、奇怪台词、杂音及不稳定对白。
- 台词开头预留干净场景底噪和闭嘴状态，并明确禁止嗯声、哼声、残字、杂音和参考样本泄漏。
- 台词调度增加自然收尾时间；提示词要求完整说完最后一个字后再闭嘴，不再用过紧的硬截止吞掉尾字。
- Companion 提交前会校验最终 H3 提示词中的对白标签数量、顺序、逐字文本和项目语言与剧本完全一致，再提交唯一一次原生生成。

### 验证

- 175 项回归测试全部通过；TypeScript 校验和 Next.js 生产构建成功。

## 0.1.70 - 2026-08-25

### 修复

- 图像创作选择 ComfyUI Z-Image-Turbo 后不再卡在 `0/0 REFERENCES`；纯文生图工作流现在无需参考图，只要填写目标画面即可提交。
- Z-Image-Turbo 使用独立的文生图界面和后端校验；支持图生图的其他模型仍然要求至少上传一张参考图片。
- 临时切换到 Z-Image-Turbo 时不再清空已经上传的参考图，切回图生图模型后会恢复显示。
- 提示词构建明确区分参考图引导的棚拍编辑与无参考图的 Z-Image 生成，本地工作流不会再收到与实际输入矛盾的参考图指令。

### 验证

- 180 项回归测试全部通过；TypeScript 校验和 Next.js 生产构建成功。

## 0.1.69 - 2026-08-25

### 优化

- MiniMax H3 视频提示词改为完整分离的中英文 Context-IR 渲染器；标准字段与参考标签保持不变，导演、时间、表演、声景和音乐约束统一使用项目所选语言。
- GPT-Image-2 分镜与参考图提示词改为先明确唯一视觉目标，并为每个角色、环境和物体参考分配单一职责，同时区分用户要求的变化与必须保留的身份、世界和材质特征。
- 九种制作风格获得更精确的静帧捕捉合同；动漫、电影级 3D 和定格动画分别使用专属线条、拓扑、材质、光照、透视及伪影排除规则。

### 修复

- Story Director 的英文静帧提示词与描述不再泄漏进 H3 视频指导，消除了重复 LOOK 段落、中英文混杂和 `points to` 等无关残句。
- 英文台词指令清理改为按完整单词匹配，`mask` 等产品词不会再被 `asks` 检测器误截断。
- 中文 H3 环境声与拟音声床提示词及提示词预算压缩内容保持中文，包括长动作连接语和保留信息回退文本。
- 所有图生视频服务边界都加入明确的无文字画面合同，禁止字幕、说明文字、标题、对话气泡、标志、水印、UI 和可读字符。

### 验证

- 178 项回归测试全部通过；TypeScript 校验和 Next.js 生产构建成功。

## 0.1.68 - 2026-08-25

### 修复

- H3 精确台词不再依赖 ASR 或音色相似度节点；音色绑定、逐字台词计划和时间轴结构仍然显式校验，模型相关的相似度分数不再误杀生成结果。
- 每轮解码台词会先裁到分镜时间槽，在剪辑边界做防爆音淡入淡出，再补齐成精确采样宽度后顺序组装；短音频不会把后续台词整体提前，额外生成余量也不会再冲出片段母带。
- 缺失、不完整、重叠、越界或短于 1 毫秒的台词时间戳，以及无效母带时长，会在 ComfyUI 入队前明确失败，不再静默丢台词或构造不可能的时间轴。
- 台词驱动轨和最终声景母带前都新增采样域硬上限，阻止累计取整或旧请求数据造成长度越界。
- 移除 ASR 删除后遗留的错误报告连线；音频输出不再连接到 Speech Finalize 仅接受字符串的报告端口。
- Story 明确禁止音乐时，四轨分离后的 Bass、Drums、Vocals 会全部丢弃，只有偏环境声与拟音的 Other 轨进入母带；允许配乐时仍保留全部非人声轨。

### 验证

- H3 音频回归测试、TypeScript 校验和 Next.js 生产构建全部通过；生成图还依据线上 ComfyUI 的 Trim、Fade、Concat、Decode、Speech Finalize 和 Dialogue Safe Master 实际节点定义及源码逐项核对。

## 0.1.67 - 2026-08-25

### 修复

- H3 音色身份相似度继续完全关闭；未执行检测时的 `0.000` 占位值不会参与是否通过的判断。
- faster-whisper-small 文本分数改为仅供诊断。中文同音字、专有名词、短句及模型识别差异不再误杀可用台词，也不会再触发第二次重复生成。
- 每轮台词会在偏快的分镜时长估算之外，按实际文本长度获得额外生成余量，减少配音放回精确片段时间轴前被稳定截掉句尾的问题。
- ASR 仍保留转写报告，并在明确识别到完整原句时自动裁掉前后空白；音色绑定、台词轮次、静默片段和无人物声环境音保护仍然严格执行。

### 验证

- 173 项回归测试覆盖 H3 音频与提示词、故事生成与交付、视频分段、角色选声、项目恢复和 Companion 导出；TypeScript 校验及 Next.js 生产构建全部通过。

## 0.1.66 - 2026-08-25

### 修复

- H3 精确台词的首个候选未达到严格 ASR 文本阈值时，会按需换用新随机种子自动重生成一次；首遍合格时不增加额外耗时，随机性的措辞失败也不再要求用户手动重跑整个视频片段。
- 第二个候选仍不合格时继续显式失败，近似台词、截断台词或即兴内容不会进入视频母带。
- 语音校验错误现在会区分真正启用的 ASR 文本分数与未启用的音色检查；没有执行说话人校验时，不再把 `speaker_similarity=0.000` 显示成疑似音色身份失败。

### 验证

- 69 项 H3 音频、提示词、故事交付、视频分段及 Companion 回归测试全部通过；TypeScript 校验和 Next.js 生产构建成功。

## 0.1.65 - 2026-08-25

### 修复

- H3 有台词片段在精确语音准备、音色绑定或 faster-whisper 模型缺失时会显式失败，不再静默降级为未经校验的 native 自由台词生成。
- 每轮台词改为独立生成并严格通过 ASR，再按照 Story 时间轴加入开场留白和轮间停顿，最后拼成完整时长的视频驱动音轨；同一角色的多轮台词不再被合成一句连续朗读。
- 旧项目的 speaker ID 哈希撞号会在当前片段内自动分配唯一 VoiceProfile ID，不再静默丢掉第二个角色的音色和台词。
- 编剧、分镜与 H3 层统一使用最多三轮台词合同，同时保留第四条作为溢出哨兵，以明确报错而不是静默截断。
- 交付审计改为按语义轮次与实际台词身份对齐；单条台词被拦截后，不再为后续正常轮次刷出连锁假错误。
- 无对白环境声改为与人物画面独立生成，混音前剔除生成声床中的人声轨；当 Story 声音规划为无音乐时会显式禁止任何配乐。

### 验证

- 107 项 H3 音频、提示词、故事、交付、改编、视频分段和尾音回归测试全部通过；TypeScript 校验和 Next.js 生产构建成功。

## 0.1.64 - 2026-08-25

### 修复

- 音色选角行改为顶部对齐；即使 Fish Audio 参考音色字段带有辅助说明，角色信息、性别与年龄选择器、参考音色输入框和试听控件也会保持统一的上边缘。

### 验证

- 音色选角回归测试和 Next.js 生产构建均成功完成。

## 0.1.63 - 2026-08-25

### 修复

- H3 对白安全混音现在会在独立生成的环境声床与目标母带因音频时钟量化而相差几毫秒时，显式补齐或裁掉环境声尾部；不再因严格采样数校验失败，也不会裁剪对白或拉伸音频。
- ComfyUI 片段重新生成后，浏览器持久缓存现在会绑定到本次任务 ID；即使新旧任务的创意指纹相同，页面刷新也不会再恢复上一版视频并停止轮询已经完成的新任务。

### 验证

- H3 音频与项目隔离回归测试全部通过，并覆盖环境声时长适配及同一创意版本重复生成；生产 Next.js 构建成功。

## 0.1.62 - 2026-08-25

### 优化

- Fish Audio 角色音色参考由持续“嗯啊哦”元音改为自然发音的校准句，补足辅音、音色过渡和呼吸信息，避免有声残留进入 H3 片尾；旧版参考会自动失效，每个角色只需按需重新生成一次。
- 音色表格缩短 reference_id 输入区，为试听播放器、重做和删除按钮预留更合理的操作空间。

### 修复

- 有台词的片段新增第二次 H3 纯声音生成：锁定第一次已经生成的画面，按每个分镜的时间、动作和空间透视生成连续环境声与拟音，再与独立校验过的逐字台词轨安全混音。
- 成片声音不再依赖仅含人声的驱动音轨提供背景层；台词、说话者和口型时间不变，同时消除台词间隙的死寂。
- 片尾处理缩短为只防爆音的 50 毫秒收口，让环境底音、天气声和动作拟音自然持续到剪辑点，又不会恢复旧版固定频率的滋滋残留。

### 验证

- 两条仙宫云 ComfyUI 实际样片均通过逐字 Whisper 转写、连续环境/拟音、尾部频谱和逐帧运动检查；H3 音频、H3 提示词、角色选声、视频分段、尾音和本地导出测试全部通过，生产 Next.js 构建成功。

## 0.1.61 - 2026-08-24

### 优化

- “改写剧本”在返回结果前会按 Story 视频 JSON 的生产边界校验；镜头缺失或跳号、台词格式错误、表演指令被写成台词、轮次过多，以及台词无法装入 MiniMax H3 15 秒限制时，会自动重新输出完整改编稿。
- 普通原稿台词可在不改变说话者、剧情事实、意图和回应关系的前提下压缩；只有用户明确要求逐字保留的台词才锁定原文，并可沿自然标点分配到相邻镜头。

### 修复

- MiniMax H3 精确台词条件现在使用工作流按 17 帧时序块对齐后的真实时长，避免未受声音约束的尾帧生成静电或滋滋声。
- Story 的 H3 提示词预留干净的环境底音收尾，并明确禁止嘶声、静电、数字残留和音频硬切。
- ComfyUI 完成的 Story 片段会在保持原视频编码的情况下清理最后 0.35 秒音频；Companion 导出和浏览器 FFmpeg 导出使用同一处理作为兜底。
- Companion 为无音轨片段生成有限时长的静音源，避免反向淡化滤镜在导出时无限等待。

### 验证

- H3 音频、H3 提示词、剧本改写、尾音清理和本地导出测试均通过，生产 Next.js 构建成功。

## 0.1.60 - 2026-08-24

### 修复

- ComfyUI 任务恢复现在同时检查任务历史、运行队列和等待队列；三处都不存在的旧任务号会被判定为已失效，不再永久误报“处理中”。
- Story 刷新后会立即解除失效视频任务锁，同时继续保留确实正在排队或运行的任务。

### 验证

- 新增运行中、排队中与不存在三类 ComfyUI 队列结构测试；H3 音频、视频分段和生产构建均通过。

## 0.1.59 - 2026-08-24

### 修复

- 单镜头的冗长 H3 视觉补充提示会在组装前按“开端—结果”压缩；权威动作、运镜时间轴、逐字台词和声音契约不被截断。
- H3 最终字数整形会先折叠重复的参考保留说明，合法的 7023 字符镜头不会再因为 7000 字符硬上限被直接拒绝。
- 动作弧压缩现在从原文真实末尾保留结果；重复长句不会再因最后的前缀截断而丢掉可见后果。

### 验证

- 新增超长单镜视觉补充回归用例，确认最终提示词不超过 7000 字符，同时保留视觉开端、结尾结果、定时动作，并且逐字台词只出现一次。
- 21 项 H3 提示词结构测试与 19 项 H3 音频测试全部通过。

## 0.1.58 - 2026-08-24

### 修复

- 视频素材预处理或 Companion 提交在返回持久 ComfyUI 任务号之前失败时，Story 不会再永久停留在“生成中”。
- 项目刷新后会把没有任务号的假生成片段解除为“失败/可重试”；如果片段首镜已有可恢复任务号，则整个片段继续保持运行并恢复轮询。
- 入队前失败会立即同步 React 状态、最新分镜引用与项目存储，不再等待 30 秒自动保存，刷新也不会复活假锁。

### 验证

- 新增两项回归测试：多镜片段没有任务号时整体解锁，以及只有片段首镜持有任务号时整段继续恢复。
- 视频分段、H3 音频测试与生产构建均通过。

## 0.1.57 - 2026-08-24

### 修复

- 多人 H3 对白会在音色档案上继续保留角色的全局 Story speaker ID，同时按照实验性 Dialogue Script 节点要求，使用当前片段局部的 `S1/S2/S3` 别名引用这些音色。
- `S3 + S1` 这类全局角色组合不再因局部 `S1` 别名覆盖而把两句台词解析到同一音色，也不会再报“joint dialogue EXP requires 2 or 3 distinct speakers”。

### 验证

- 已依据仙宫云实际安装的 ComfyUI 节点实现复现片段三的 `A-Luo=S3`、`人鱼公主=S1` 顺序，并新增回归测试：音色档案保持全局 `S3/S1`，对话脚本正确编译为局部 `S1/S2`。

## 0.1.56 - 2026-08-24

### 修复

- 精确台词生成现在按剧本中不同 speaker ID 的数量选择单人或多人条件节点，不再按上传了多少份音色参考文件判断。
- 同一角色重复的音色参考以及没有在当前片段开口的角色参考，会在进入 H3 条件节点前去重，避免单人台词被误送进 `MiniMaxH3JointDialogueConditioningT8` 后遭到拒绝。

### 验证

- 新增“两份遗留音色参考、同一 `S2` 角色连续两句台词”的回归用例；编译后的工作流只有一个音色档案和单人条件节点，不含 Joint Dialogue 节点。
- 已确认上一段第 2 片段在生产 ComfyUI 实例成功完成，并保留原始的 `S2 · Tide Officer` 绑定。

## 0.1.55 - 2026-08-24

### 修复

- 首尾帧续接片段同时使用已校验的精确台词驱动音轨时，MiniMax H3 现在会选择 `Hybrid` 条件模式，不再以不兼容的 `Ref2VA` 任务进入执行并报错。
- H3 音色档案会在 Story 提示词、Fish Audio 音色参考和精确台词条件节点之间保留剧本中的 speaker ID。只有 `Subject 2` 开口时不会再被重新编号成 `S1`，避免作为静默听者的人鱼公主继承 Tide Officer 的男声和口型。

### 验证

- 已对照最近项目真实的 ComfyUI 提交历史确认两项错误，包括提示词中的 `Subject 2 / S2` 与音色档案中 `S1` 的错位。
- H3 音频、提示词结构、视频分段测试，以及生产网站与 Companion 构建均通过。

## 0.1.54 - 2026-08-24

### 新增

- 将官方 ComfyUI Z-Image-Turbo BF16 文生图工作流接入为全局图片服务商，Story 九宫格、单张分镜、角色草稿、定妆图和场景参考图均可选择。
- 图片任务使用独立的 `comfyui-image:` 标识，可在刷新后通过本机 Companion 恢复，并按环境返回持久 Cloudinary 地址或无损本地 PNG 数据。
- 全局模型选择器明确显示 Z-Image-Turbo 的真实能力边界：支持文生图；需要参考图编辑时继续使用 APIMart，不会静默丢弃编辑输入。

### 优化

- Z-Image 不能读取视觉参考时，会保留角色与物体的文字设定；九宫格提示词使用更长预算，避免后几格因 APIMart 的短提示词限制被截断。
- Companion 跨域白名单覆盖网站所需的全部静态图片生成与状态查询路由。

### 验证

- 已在 49GB 4090D 的仙宫云 ComfyUI 安装官方 BF16 扩散模型、Qwen 3 4B 文本编码器和 Flux VAE；三个文件尺寸与官方一致，并在队列安全重启后被 ComfyUI 正确识别。
- 使用 8 步工作流实际生成并下载一张 1344×768 验证图；生产网站构建、Companion 构建、图片模型、角色设计、九宫格恢复与 Companion 跨域测试均通过。

## 0.1.53 - 2026-08-24

### 优化

- Story 在全片因果骨架之后一次性撰写完整台词稿，并在逐镜执行前锁定说话者、逐字台词、语义证据、潜台词和听者变化，避免关键对白退化成互不连贯的短口号。
- 全片明确保存七个结构节点、观众认知变化和镜头剪辑交棒，并贯穿剧本、分镜、H3 提示词、片段缓存签名和交付审计。
- 详细剧本改为逐镜补充表演、状态和声音执行，但每镜事件与台词仍由全片稿统一授权；模型返回不完整数组时不会再拖垮整部长片，也不会割裂故事。
- H3 蒙太奇提示词保留合并片段内每个分镜的秒数、具体动作、运镜、逐字台词和有动机的物理转场；兼容镜头仍会在 15 秒限制内自动合并。

### 修复

- APIMart GPT-4o 输出预算限制在服务商 16K 上限内，18–81 镜长片不会再因最后一批只返回一镜而整体失败。
- 英文项目遇到执行模型翻译了已被全局骨架覆盖的临时字段时会确定性修复，不再丢弃已经正确锁定的英文剧情与台词。
- Story 交付门禁会在生成视频前定位缺失结构节点、剪辑交棒、缩短的锁定台词、丢失语义和错序对话。

### 验证

- 最近项目完成一次真实 18 镜英文改编：7 个结构节点、23/23 条锁定台词、18 张 1:1 分镜，端到端故事交付审计为 0 错误。
- 自动剪辑得到 14 个 H3 片段，其中 4 个为多分镜蒙太奇片段；全部不超过 15 秒。Story、交付、分段、H3 相关测试与生产构建通过。

## 0.1.50 - 2026-08-24

### 修复

- 角色音色参考改为带版本的 Fish Audio 无语义音色探针，普通示例句中的单词不会再泄漏到 MiniMax H3 台词开头；已保存或导入的旧版有语义参考会自动失效，每个开口角色只需重新生成一次。
- Story 台词改为原生 H3 两阶段链路：先由 H3 按锁定音色生成剧本中的逐字台词，经 faster-whisper 校验并裁切，再用这条已验证的 H3 音轨驱动最终 Ref2VA 表演和口型，同时由 H3 重混环境声。
- 最终视频提示词会把已验证音轨视为权威对白，而不是宽松的音色参考；导演说明、参考音频音节、旁白和即兴词不会进入人声层，画面动作对应的环境声与拟音仍可保留。

### 验证

- 使用既有分镜实际重跑一段 13.67 秒、1:1 的 Story 样片；Whisper 只转写出 Tide Officer 的两句指定台词，没有开头参考词、动作旁白或额外人声，台词结束后的尾部仍保留非静音环境声。
- TypeScript、Story/H3/音色/分段相关回归测试和 Companion 生产构建通过，H3 缓存契约升级为 v13。

## 0.1.49 - 2026-08-24

### 优化

- Story 会先完成全片因果骨架，再按较小批次撰写详细剧本；每镜保留原始镜头映射、戏剧变化、观众信息、对白功能、动作、运镜、声音和物理衔接，长篇改编不再退化成彼此孤立的漂亮画面。
- 分阶段编剧提高了结构化输出预算，按对白密度切分批次，并增加有上限的自动纠错重试；18–81 镜项目不会再因为外层 JSON 被截断而误报“返回 0 镜”。
- 剧本发现的每个开口配角都会进入有效制作角色表，拥有可复用角色卡、明确的性别/年龄/身份、确定性的 Fish Audio 音色以及 Story 内的音色操作入口；自动失效转移不会跨越男女声池。
- MiniMax H3 会收到片段内每个分镜的完整时间契约：具体动作阶段、运镜、逐字台词、环境声、因果拟音和物理转场。多分镜密集片段使用无损精简格式，保留必要信息并控制在 H3 提示词上限内。

### 修复

- 必要台词会贯穿骨架标准化、详细剧本、分镜导演和片段合并；导演说明、未绑定临时人物和重复台词会在成为可听人声前被拦截。
- 剧本自动生成的配角现在可以参与分镜生图、角色参考卡、音色参考和项目断点恢复，不再出现空白角色卡或错误继承主角声音。
- 进入分镜导演前新增故事交付审计，缺失的因果、角色绑定或必要台词会在剧本阶段明确定位；内容安全拒绝会自动改写成非血腥的剧情表达，同时保持用户锁定事实和台词不变。
- 开发环境的 Companion 允许其他本机端口访问 Story，生产环境的来源限制保持不变。

### 验证

- 新增故事交付、分阶段剧本、群像音色与 H3 提示词预算回归；打标签前通过 TypeScript、生产构建和 Companion 发布测试矩阵。

## 0.1.40 - 2026-08-23

### 优化

- “改编剧本”不再强制所有新增台词都写成短句，而是先规划完整的场次对白单元；提问、拒绝、决定、承诺与回收会跨镜共享上下文，并明确禁止脱离指代的孤立口号。
- 剧本与导演契约新增对白单元、必要性、听者变化和蒙太奇句法；承担关键信息的必要台词不能在详细剧本阶段静默降级为纯视觉镜头。
- H3 v6 继续保留合并片段中每镜的实时动作和物理转场，只把可见的听者表演传给视频模型，抽象剧本说明仍与视听通道隔离。

### 修复

- 每个有台词的角色现在会在全片锁定同一个、符合角色资料的 Fish Audio 音色；年轻女性美人鱼会自动匹配年轻女声，用户手选音色保持最高优先级，旧项目在加载或导入时自动迁移。
- Fish Audio 台词和角色音色参考接口会拒绝缺少 `voiceId` 的请求，不再静默使用可能改变性别或角色身份的默认声音。
- 每条权威台词都会保存角色音色并纳入 H3 缓存签名，旧版错误男声生成的片段和锁定音轨不会被重新复用。

### 验证

- 新增自动音色选角、用户音色保留与缺失音色阻断测试；Story、H3、分段、无人托管、恢复、导出及 Companion 路由测试和生产构建均通过。

## 0.1.39 - 2026-08-23

### 优化

- 剧本原文中的明确台词会作为规划、分镜导演和合并 H3 片段的权威输入；剧情确有对答需要时，单镜最多保留四条有序台词，旁白标签和表演说明不会再被当成说话者。
- H3 片段时长会预留与精确音轨编译器一致的开口前、收尾后留白，并向上取整所需秒数，避免合并台词被挤进过短片段。

### 修复

- 九宫格、拆分分镜、剧本更新、视频缓存完成、项目导入和暂停状态都会立即持久化，不再等待下一次定时保存。
- 当前项目改用带版本的权威存储键；旧部署留下的标签页仍可更新兼容数据，但不能再覆盖最新分组、视频任务号和已完成时间线。
- “一键成片”暂停后立即继续时，会先等待旧编排锁退出，再从第一个未完成阶段启动接替任务，不再停在等待状态。
- 导入项目时会同步刷新内存中的故事计划并立即落盘，断点续跑不会引用导入前的旧计划。

### 验证

- 以最近的 1:1 Story 项目完整重跑：27 张新分镜、16 个合并 MiniMax H3 片段、本地缓存及 Companion FFmpeg 合并全部成功，得到 158.21 秒、960×960 成片。
- Whisper 复核覆盖全部剧本权威台词，没有额外中文台词、表演指令或不明人声；Story 分阶段、片段编排、H3 提示词、无人托管和项目隔离测试以及生产构建均通过。

## 0.1.32 - 2026-08-23

### 优化

- Story 的执行编剧与分镜导演阶段融合了 `video-shotcraft` 中适合本产品的导演原则：每镜一个主动作弧、加速进入明确触点、结果短回落、相机服务情绪，以及每个接缝只使用一种物理交棒语法。
- H3 片段会为合并后的每个分镜保留完整的秒数、动作、运镜、台词和物理衔接，并延续运动矢量、速度与银幕方向，不再依赖叠化或装饰性漂移。

### 修复

- 不再把剧本中的前因、压力、选择和观众理解等抽象解释重复塞进 H3 视听描述，避免 Ref2VA 把本应静默的导演说明误念成额外旁白。
- H3 提示词会声明本段唯一允许的可理解人声数量；只有时间表中带标签的精确台词可以发声，旁白、即兴词、歌唱、参考音频原话和背景可辨识人声全部禁止。
- H3 提示词契约升级后会自动让旧引擎生成的片段缓存失效，断点续跑不会继续复用带旧台词或旧动作规则的视频。
- 剧本与多分镜 H3 生成统一要求 Companion v0.1.32，确保线上前台和本地生成接口使用同一套导演与声音规则。

### 测试

- 新增静默解释文本、精确 H3 人声事件数、ShotCraft 动作/衔接规则和提示词契约缓存失效测试；109 项测试、TypeScript 校验和生产构建全部通过。

## 0.1.31 - 2026-08-23

### 修复

- 27–81 镜长剧本现在要求 Companion v0.1.31，避免本地长请求被旧版剧本接口接管并静默丢失核心悬念、观众信息、对白功能和蒙太奇字段。
- 全片骨架的输出预算会按新版逐镜叙事字段扩容，避免 27 镜规划在 JSON 结尾被截断后误报“返回 0 镜”。
- 故事骨架、逐镜剧本和导演批次改为三次带短退避的尝试，APIMart 瞬时 TLS 重置或超时不会立即耗尽全部纠错机会。
- Companion 的公共 DNS HTTPS 通道若在收到响应前失败，APIMart 剧本请求会自动回退系统网络栈；既保留污染 DNS 电脑的兜底，也不会破坏本来依赖系统代理的电脑。
- 证书主机名不匹配会被识别为公共 DNS 污染；首次出现这类错误或响应前重置后，本次 Companion 进程的后续编剧/导演批次会持续使用已验证的系统网络链路。
- 导演 description 中重复出现的精确台词会在校验与合并前被确定性移除，不再依赖模型反复改写，也不会把对白复制进可能被 H3 念出的视觉说明。
- 台词剥离兼容句号、破折号和引号差异（例如 `Again.` 与 `“Again—”`），并清除孤立西里尔残词；整段输出语言错误仍会被拒绝。
- 导演批次会拦截中英文串写、异常西里尔残词，以及在画面描述中重复精确台词的输出；不合格批次会在进入生图与 H3 前自动纠错重试。
- 台词归属以精确的画面出场角色列表为权威；英文 action 可以自然写成 “the mermaid princess”，音色绑定仍保留上传角色名“人鱼公主”，不会因叙述别名误判失败。
- 总纲中任何非纯视觉对白功能都必须先绑定已上传角色；没有有效音色归属的规划会改为可见剧情信息，不再在“缺台词”和“临时角色禁止发声”之间循环失败。
- 模型若在合法台词旁额外添加临时人物回应，剧本会保留已绑定角色台词并只删除无归属内容，不再让整批失败，也不会合成陌生音色。
- 仅由 AI 规划、没有用户锁定原文的对白若无法生成合法绑定，可回退为视觉回收；用户锁定台词仍强制保留，兼顾不中断流程与不擅自编造替代台词。
- “一键成片”新增浏览器级、按项目隔离的托管锁；同时打开或刷新多个 Story 标签页时，不会再重复提交同一付费生图/视频片段，只有原托管页关闭或崩溃后候补页才会自动接管。
- 某个标签页正在托管一键成片时，其他旧标签页会暂停周期性自动保存，避免用旧快照覆盖最新分段方案、任务号和缓存状态。

### 测试

- 新增导演语言、精确台词隔离和跨标签页托管锁回归测试，并以最近的 27 镜项目执行完整“一键成片”验收。

## 0.1.30 - 2026-08-23

### 优化

- Story 骨架新增全片核心观众问题、观看承诺、对白弧线与蒙太奇策略，并把它们传入每个场次；每镜必须带来明确的观众信息增量，同时留下推动下一镜的问题。
- 剧本 beat 明确区分可见动作、戏剧变化、信息增量、对白功能和蒙太奇语义；问题、揭示、选择、承诺与回收会跨镜规划，不再生成孤立口号或一串漂亮但并列的事件。
- 有叙事必要的对白不再被“每镜最多一句”的旧规则一律压缩；在动作与 15 秒预算允许时，一镜可以容纳两条严格按顺序、绑定音色的“发起→回应”或“揭示→决定”台词。
- 导演与 MiniMax H3 提示词会逐镜编译剧本的触发、压力、可见选择、后果和观众应理解的信息；因果、平行与对照蒙太奇会驱动物理镜头衔接，不使用淡入淡出，也不补画外解释。

### 修复

- H3 片段不再让静态分镜生图 prompt 覆盖编剧锁定的动态 action。
- 同一分镜的多条台词会按顺序安排开口留白、回应间隔和说后反应；只有精确台词能进入 H3 的对白标签。
- 已规划对白功能但缺少说话者、可朗读原文、叙事功能或画面内角色时，剧本阶段会判定无效并结构化纠错重试，不再到下游静默变成无台词或断裂台词。
- H3 缓存签名新增因果故事、信息增量、对白功能与蒙太奇字段；修改叙事后不会复用旧片段。

### 测试

- 新增故事/观众契约、单镜双台词时间表、H3 因果编译和叙事版本缓存失效回归测试；103 项测试、TypeScript 校验和生产构建全部通过。

## 0.1.29 - 2026-08-23

### 新增

- “一键成片”升级为可断点恢复的无人托管流程，会依次完成剧本/分镜、制作参考、分镜图、分组 H3 片段、本地缓存、Companion FFmpeg 合并与最终下载。
- “一键成片”新增真正的暂停/继续：暂停会保留已完成产物和已提交的供应商任务号，继续时从第一个真正未完成项开始。

### 修复

- 一键成片遇到短暂的生图、视频、Companion 或导出失败后不再静默退出；可恢复阶段按 3/8/15/30/60 秒有上限退避持续重试，直到成功或用户明确暂停。
- 已存在的分镜图 URL 成为完成状态的权威依据；即使旧项目状态仍显示等待或生成中，刷新与续跑也不会重复生图。
- 中断的付费 3×3 生图会重连同一个 APIMart 任务并重新拆分结果；部分完成但母图任务已不可恢复时，只单独补齐缺失分镜，不覆盖成功图片。
- 单张补图任务与九宫格任务会立即持久化并明确区分，避免刷新后把单张任务误当成九宫格恢复。
- 保存的 ComfyUI 视频任务号在网络超时后会继续轮询；只有供应商明确返回终止失败时才允许提交替代任务。已完成片段会跳过，本地成片导出也会自动重试。

### 测试

- 新增“产物优先”续跑、付费九宫格重连、局部单镜补图、任务类型隔离和无人托管退避时序回归测试；九宫格恢复、本地原生导出与生产构建均已通过。

## 0.1.28 - 2026-08-22

### 修复

- Story 视频片段新增确定性的“制作输入签名”，覆盖分镜图、动作、运镜、台词、时长、连续性、画幅和制作风格；修改或重新生成任一输入后，受影响片段会自动失效，不再静默复用旧视频和旧音轨。
- 视频持久缓存键除了项目与分镜身份，还会包含本次生成签名，避免同一项目早期片段把旧版旁白串入重新编辑后的成片。
- 4K 九宫格分镜的轮询上限由 4.5 分钟延长到 9 分钟，避免 APIMart 付费任务仍在正常生成时前端提前误报超时。

### 测试

- 新增制作版本缓存隔离、分镜图或台词变化后自动失效片段的回归测试；生产构建、H3 音频/提示词、本地导出、九宫格恢复、片段编排和项目隔离测试全部通过。

## 0.1.27 - 2026-08-22

### 优化

- Story 在展开分镜前会为每句生成台词绑定明确的已上传角色；临时人物或无名人物的内容只保留为可见表演，不再错误转交给主角说出。
- MiniMax H3 片段提示词改用官方六段式 Ref2VA 契约及官方镜头/时间戳语法；同一片段里的每个分镜仍完整保留动作、运镜、秒数、镜头衔接和授权台词，同时移除可能被 H3 误读成语音的自定义伪控制字段。
- 仙宫云 MiniMax H3 T8 节点已由 v1.3.2 升级到 v1.43.0；AID 三套 H3 工作流的核心节点均保持兼容，旧版节点也已移出扫描目录并保留备份。

### 修复

- 如果生成台词的说话者没有出现在当前分镜中，系统会自动隔离该台词：前台显示具体原因，后端不会把它写入 `<d>`，也不会用它生成声音参考。
- 剧本规划会校验“必说台词”和有效角色归属；模型返回无说话者或错误人物时自动进入纠错重试，不再把归属错误传递到分镜、生图、视频和原生音频阶段。
- 视频提示词覆盖会清除旧项目残留的 `SPEECH GATE`、`DIALOGUE`、`SPOKEN_WORDS_ONLY` 和 `NON_SPOKEN_PERFORMANCE` 字段，避免导演指令再次被当成对白。

### 测试

- 新增缺席角色台词隔离、剧本说话者校验、H3 官方镜头格式和旧伪控制字段清理的回归测试。

## 0.1.26 - 2026-08-22

### 优化

- Story 自动片段编排会按每镜真实动作/对白时长、最多 4 镜、最多 3 条对白和 H3 15 秒上限组合连续分镜；换地点、换 sequence 或模型写出的淡入淡出不再机械地把每镜拆成独立片段。
- 合并后的每个分镜仍保留独立时段、动作、运镜、对白与因果衔接，只改变 H3 的生成批次。

### 修复

- 手动拆分、合并和边界调整现在保存为项目级“导演编排”，操作后立即持久化；刷新页面、重新打开项目、无限画布和“一键成片”都会沿用同一分组，不再恢复成自动单镜分段。
- H3 可见动作、分镜描述和用户覆盖提示词会清除夹带的引号台词及“喘息着喊”等发声指令，真正对白只允许在权威 `<d>` 标签内出现一次。
- 配置 Fish Audio 时，Story 会为当前片段生成只包含准确台词的角色声音参考，并按台词签名缓存；修改台词后会自动失效重建，不再把通用音色样本中的示例句带入成片。
- Companion 会将不足 2 秒的准确台词参考补静音至 H3 最低长度，同时继续限制全部参考音频不超过 14.7 秒，避免短台词生成失败或回退到错误声音。

### 测试

- 新增自动跨场景合并、15 秒预算、手动导演编排序列化恢复、视觉通道台词清理、准确短台词参考补齐等回归测试；全套测试和生产构建通过。

## 0.1.25 - 2026-08-22

### 优化

- Story 会把编剧阶段每个 beat 的权威可见动作完整带入分镜，不再要求最终视频提示词从静态生图描述中猜测动作。
- 合并到同一个 MiniMax H3 片段的每个分镜都会获得独立的精确时段、动作节奏与落点、运镜、台词状态、可见动作声和有动机的电影化衔接。合并只改变生成批次，不再压缩任何分镜内容。
- H3 动作时间表会直接进入运动，在末段反应窗口前完成关键接触或转向，并以真实物理速度保留次级运动，避免把一个动作拉长成类似慢镜头。
- 首尾帧连续模式把尾帧作为动作落点：主体动作在最后 16% 之前完成，不再用整段时间在两个静态姿势之间均匀慢插值。

### 修复

- “先短暂停顿，再以坚定语气说”等模型生成的表演说明会在保存或重新生成前从台词中剔除；若包含引号，只保留引号内真正说出口的话。
- 情绪、停顿和说话方式会转换为不可朗读的 H3 结构化控制码，只有权威 `<d>` 标签内的文字允许发声。
- 分镜参考图只锁定身份、服装、地点与光线，不再锁死姿态、调度与机位，使每个分镜可以完整执行动作和运镜。
- 四分镜 H3 完整结构保持在供应商 7000 字符限制以内，同时不丢失动作、运镜、台词状态或镜头衔接。

### 测试

- 新增合并片段逐镜时间表、完整动作/运镜/台词契约、电影化衔接、首尾帧节奏、表演指令过滤和 H3 提示词预算回归测试。

## 0.1.24 - 2026-08-21

### 新增

- 全局生图模型新增 APIMart Nano Banana 2（`gemini-3.1-flash-image-preview`）和 Grok Imagine 2.0 官方版（`grok-imagine-image-2.0`），Story 九宫格与单镜、人物/场景素材、角色设计和图生图均可使用。
- 独立角色设计工作台保存的角色会自动进入 Story“历史角色”，并携带完整角色卡、身份、年龄、性格、主题、服装和制作风格供后续项目复用。

### 优化

- 九种制作风格分别获得明确的媒介/材质、可解释光源、镜头/景深和色彩契约，不再只依赖泛化的风格标签。
- “自然电影”进一步强调相机或手机直拍的皮肤毛孔、发丝、织物、有限动态范围、真实光学景深和克制镜头特征。
- 九宫格提示词保留结构换行，并优先在单词边界裁剪逐格描述，同时完整保留九个画面身份与精确人物约束。

### 修复

- 统一 APIMart 生图适配层会正确处理 Grok 的 `aspect_ratio`、响应版本头、2K/三张参考图上限和对象式任务 ID，同时保留 Nano Banana 2 的 4K 与十四张参考图能力。
- Story 会同步限制参考图和提示词中的编号，避免供应商参考图数量较少时出现 `Reference image N` 缺失或错位。
- 图生图与角色设计会根据当前模型调整参考图数量，不再提交模型不支持的请求体。

### 测试

- 新增全局模型选择、供应商专用请求体与任务 ID、Story 角色库迁移、不同风格成像契约和九宫格提示词结构的回归测试。

## 0.1.23 - 2026-08-21

### 新增

- 首页新增独立“角色设计”工作台：可组合文字简报与最多 4 张参考图，探索 4 款或 9 款可选形象，并把锁定方案扩展成可用于生产的 4:3 完整角色卡。
- 完整角色卡会把身份、年龄、性格关键词和核心主题带入转面、轮廓、表情、姿态、材质、动作细节与连续性色板模块。
- 角色草稿同时支持 2×2 与 3×3 高清拆分、下载、重新生成和本地角色库存储。

### 修复

- MiniMax H3 台词提示词改为官方格式：说话者身份、动作和表演方式放在 `<d>` 外，`<d>` 内只保留真实语言标记与逐字对白。
- “无其他角色在场”“其他角色保持沉默”等导演指令不再作为 speech 字段要求、不再重复写进声音总览，旧项目中的同类脏台词也不会进入 H3 对白。
- `overall_soundscape` 只保留环境声、可见动作声和明确要求的非语言背景人声，不再混入台词控制说明。

### 测试

- 新增 H3 导演指令泄漏、逐字台词位置、4/9 款角色草稿、角色卡元数据以及 2×2/3×3 拆分回归测试。

## 0.1.22 - 2026-08-21

### 修复

- 单镜头导演批次不再把合法的单元素 JSON 数组误截成对象，继而错误提示“返回 0 镜，要求 1 镜”。
- 分镜导演会先兼容直接镜头对象以及常见的 `shots`、`storyboards`、`items`、`data` 供应商包装，再严格校验目标镜头数量。
- 结构化响应提取会保留真正的 JSON 外层结构，兼容代码块和前后说明，并正确处理字符串内部的括号与大括号。

### 测试

- 新增单元素数组、单镜头对象、嵌套 JSON 标点和常见供应商包装格式的回归测试。

## 0.1.21 - 2026-08-20

### 优化

- Story 现在会先锁定精简的全片故事骨架与精确镜头地图，再按场次以最多 9 镜一批展开详细剧本，最后使用相同批次边界生成导演分镜提示词。
- 每个批次都会获得全片故事脊柱、上一批交接状态和后续两镜路线。同一场次严格延续人物调度与光线；新场次可以明确重建时间、地点、构图和光源，同时保留人物与剧情状态。
- 原“AI 扩写”升级为“改编剧本”：根据当前选择的 9–81 镜制作规格和预计片长，把原始内容预先改编为数量精确、编号连续且前后因果相连的剧情节拍。

### 修复

- 长片不再要求 DMX 在一次响应中同时返回最多 81 镜的故事结构、详细剧本、声音规划和摄影提示词；每个阶段现在具有独立的输出字数与超时预算。
- DMX 请求超时后不再通过另一个 OpenAI 接口重复发送同一份过大的内容，避免连续等待两个 4 分钟超时。
- JSON 格式或镜头数量不正确时会在对应批次内自动纠正重试，并报告准确镜头范围，不再无法定位地丢弃整份剧本。

### 测试

- 新增分阶段生成回归测试，覆盖精确镜头配额、连续编号、场次安全的 9 镜分批、提示词阶段隔离与导演批次一致性。

## 0.1.20 - 2026-08-20

### 优化

- Story 分镜导演不再堆叠泛化的“电影感”词汇，改为明确相机距离、镜头透视、构图与遮挡、焦点平面与景深、可解释光源、有限曝光、材质响应和符合成像系统的细微镜头特征。
- 项目制作风格现在会贯穿导演与静帧生图链路；连续镜头共享相机/渲染家族和色彩响应，同时保留每镜独立的光影、景深与透视差异。
- 九宫格提示词改为“精简项目成像基线 + 每格摄影差异”，总预算限制为 3500 字符；裁剪重复连续性说明前，优先完整保留九个场景身份、精确人物数量与核心成像物理。

### 修复

- 半透明强调色选中卡片不再误用实心按钮的深色文字与实心 Hover；Story 画幅选中项会正确显示主题色文字和图标。

### 测试

- 新增静帧摄影物理、九宫格精简提示词、供应商字数预算与 Panel 1–9 完整保留的回归测试。

## 0.1.19 - 2026-08-19

### 新增

- Story 导出会先把所有 H3 成功片段逐个保存到 Companion 的持久目录，再启动 FFmpeg；刷新浏览器不再丢失成片源文件。
- 本机导出任务会持久记录片段清单、中间文件、进度和最终成片，Companion 重启后可以从未完成步骤继续。

### 修复

- Story 导出不再卡死在浏览器 FFmpeg WASM 的 5%；Companion v0.1.19 改用随应用打包的原生 FFmpeg 合并。
- 片段下载、逐段标准化和最终拼接分别最多自动重试 3 次；SHA-256 校验成功的片段及有效中间文件会直接复用。
- 合并前统一分辨率、24fps、像素格式与音频规格；没有音轨的片段会补静音轨，降低长片拼接失败概率。
- 未完成任务会保留浏览器恢复标记，再次进入同一项目的导出页后自动续跑；最终文件只在本机合并完成后传回，不再进入浏览器 WASM 内存。
- 成片交付改用浏览器 Blob 下载，避免 HTTPS 页面直接跳转 localhost 被 Chrome 拦截。
- 关闭本机 Companion 时使用的浏览器备用导出增加加载超时和明确提示，不再永久停在“准备 FFmpeg”。

## 0.1.18 - 2026-08-19

### 修复

- DMX 的 GPT-5 系列剧本模型现在优先使用官方 Responses API，仅在需要时回退到 Chat Completions。
- 剧本接口兼容标准 Chat Completions、内容数组、结构化 parsed 输出、Responses API 与常见网关包装，不再只识别 `choices[0].message.content`。
- DMX 空响应会区分 token 截断、模型拒绝、传输失败与返回结构，并且不会把提示词、生成内容或密钥写入日志。
- 检测到旧版 Companion 时会明确要求更新 v0.1.18，不再把长剧本请求静默转到容易超时的托管接口。
- 分镜生图现在会在提交前定位高风险镜头，生成独立的非血腥生图提示词；APIMart 安全拒绝后会逐级降低伤害表现并自动重试。
- 九宫格与单张分镜都会保留原始 prompt，同时在卡片上显示诊断原因、候选镜头和自动重试次数。

## 0.1.17 - 2026-08-19

### 优化

- Story 的九种制作风格现在分别向 MiniMax H3 最终提示词写入表演、动作、剪辑和声音规则，不再主要停留在色彩预设。
- “自然电影”明确模拟真实相机或手机直拍：有限曝光宽容度、真实对焦恢复、手持惯性、滚动快门以及皮肤和织物细节；“观察纪录”则独立强化手机/随身相机的现场感。
- 每种风格的声场均约束为画面可见动作所导致的环境声和拟音，避免莫名台词与装饰性声音。

## 0.1.16 - 2026-08-19

### 新增

- Story 视频阶段升级为“片段编排”：AI 自动把连续分镜组织成 1–4 个节拍、最长 15 秒的 H3 片段，并支持点击拆分、合并和双向调整边界。
- 无限画布改为故事 → 九宫格批次 → 独立分镜 → H3 片段 → 最终时间线的结构化流程，不再把一个分镜机械对应成一个视频。
- 九宫格改为 4K 母图生成；拆分阶段保留高清母图，并对每格执行裁切、1600px 有效边限制和质量优先压缩，H3 提交前再做画幅与 1.6MB 体积预处理。

### 改进

- MiniMax H3 提示词采用官方 Base/Ref2VA 结构：统一引用定义、保留关系、逐镜时间线、稳定说话人编号、环境声和配乐字段。
- 每个 H3 片段明确建立、推进、转折和落点，镜头衔接使用可见因果、动作承接、银幕方向和物理状态，强化电影节奏与整体剧情感。
- 视觉风格、角色数量、道具状态和表演动作改为正向约束，减少会稀释主动作的重复负面关键词。
- 台词采用 H3 的 `<d>[Language]…</d>` 结构，声音参考只绑定音色与表达；无台词镜头保持非说话表演与可见动作拟音。
- 禁止画面文字的规则收敛为一次简洁的干净画面约束，不再在每个分镜重复字幕相关词汇。

## 0.1.15 - 2026-08-18

### 修复

- 剧本 API 新增“自动 / 仅 DMX / 仅 APIMart”明确选择；仅使用模式不再静默切换供应商。
- Companion 为 DMX 和 APIMart 使用独立公网 DNS，绕过代理/VPN 的 `172.19.x.x` Fake-IP 解析。
- 本地剧本接口中断时直接显示 Companion 错误，不再回退到容易超过 60 秒限制的托管函数并最终返回 HTML 网关页。
- 自动模式会同时保留 DMX 与 APIMart 的失败原因，便于判断 Key、模型或网络问题。
- 图生图参考素材改为浏览器端压缩并逐张上传，生图接口只传 URL，避免多张 Base64 原图超过网关请求体限制。
- 图生图、角色定妆和单张分镜生成统一安全解析空响应、HTML 网关页和 JSON 错误，不再显示 `Unexpected end of JSON input`。

## 0.1.14 - 2026-08-18

### 修复

- Story 连接测试不再在新电脑上下载并解析无关且体积较大的视频换人物工作流。
- 连接测试分别验证设备密钥、H3 工作流与 ComfyUI API，并在浏览器/本地网络中断时显示可执行的诊断信息。
- SSH 地址、端口或实例不可用时，连接测试会快速返回明确错误，不再长时间卡住后显示 `Load failed`。
- Companion 授权和 Story 正式连接现在统一使用公网 DNS 解析，修复代理/VPN 将仙宫云域名解析到内网假地址后“显示已授权但实际连接失败”的问题。

## 0.1.13 - 2026-08-18

### 修复

- 连续镜头改为在终止静帧前交接运动，并在合并时同步裁切接缝两侧。
- MiniMax H3 只允许剧本中已确认的台词和必要的轻量现场声；声音参考不再被当作台词内容。
- 图片与视频提示词增加逐节拍精确出场人数约束，避免人物重复或额外生成。
- 剧本规划默认采用视觉叙事，不再自行添加台词、旁白、字幕或无来源人声。
- Companion 明确采用每台电脑独立的设备密钥，并提示新电脑需要分别授权。
