# AID 0.1.213

修复中枢统一区分文字校验、原任务续查、素材恢复、临时拥堵、审核拒绝与未知错误。独立 Story 不再无限重试；预算和处理记录随项目/写作缓存持久保存，页面可查看工作流处理与停止原因。编剧、导演沿用已有局部字段修稿和原校验器，保存旧稿版本；未知原因、审核拒绝和不确定提交不会触发自动改写或新生成。

修复道具纠错可能把“潘达面膜”中的“面膜”替换成“普通大袋子”的问题：保护全部已登记道具名称，拒绝含糊重复短语的全局替换，修稿上下文增加道具描述。新增有原文依据、精确旧值校验、暂停队列要求的视觉文字修复工具，归档受影响镜头，只重写指定镜头的视觉提示，保留其他付费素材和任务编号。

ComfyUI 视频现在复用原音频缓存修复失效的旧 Cloudinary 声音链接，原有效/内嵌音频直接使用。试音服务再次读取旧缓存时也检查旧托管链接，失效只补上传，不重新合成或更换音色。新总纲人物外貌字段与剧情、人物变化和声音表演分开。

30组613项发布检查通过，Companion/Netlify构建与实际安装发布记录在 `out/releases/v0.1.213/`。Netlify第二次构建曾遇到扩展查询403，登录与站点绑定确认有效；最终部署状态以发布回执为准。

《为自己》真实全流程仍在验收：素材全部完成，分镜阶段发现并修复1、2、14、15镜的电脑/道具归属和包装名称错误；保留原台词、时长、音色、模型及指定产品参考。生成结果审核无法保证永不拒绝，通用语义错误也不能仅靠格式校验彻底排除。详情见 `docs/weiziji-e2e-20260908.md`。
