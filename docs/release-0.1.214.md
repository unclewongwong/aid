# AID 0.1.214

局部视觉重写现在将指定镜头编号传到导演后端，只生成这些镜头，返回结果按原镜头编号合并。拒绝缺镜、重复或多余镜头；其他已生成素材与任务保持不变。修复《为自己》仅需修改1、2、14、15镜却额外请求9–10镜的问题。

独立纯文字拒绝不再当作JSON格式错误重试。初稿与局部字段修稿都保留拒绝状态，重启不能重新消耗请求；有效JSON内的角色拒绝台词仍是正常数据。明确的供应商拒绝信息仍优先处理。本次没有提高重试预算，也没有更换用户模型。

30组618项发布检查通过，包含实际调用边界、局部返回合并、缓存恢复和拒绝停止回归。构建、安装及发布回执位于out/releases/v0.1.214。真实项目仍须接续验证到成片，代码检查通过不等同于全流程完成。
