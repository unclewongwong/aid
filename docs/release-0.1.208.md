# AID 0.1.208

- 每段默认裁掉开头 0.24 秒，覆盖第一段及独立生成的镜头；音视频同步裁切。生成工作流已经裁过头部的 Motion Context 接续片段不重复裁切，独立镜头不额外裁尾部。
- APIMart 视频提交前恢复失效的托管音色链接：优先复用本地缓存原音频，上传到可用存储，保持音色 ID 和内容；没有原音频或服务暂时不可用时明确报错，避免反复提交无效付费请求。

保持原视频的光晕和采样设置。《花少之——我是大姐》两集均已复用各自原 16 段完成片头裁切并替换交付，未重新生成视频。第二集等待原生产正常完成后处理，未打断生成。

验证：片段裁切 37 项、节奏 6 项、Wan 音色恢复 11 项测试通过；网站与 Companion 正式构建通过。本机首次构建遇到旧缓存的路由模块缺失，清理生成目录后成功。

发布提交 `9ab8abb293b4cf0773cdf25cdfd31fe3418a3722`，标签 `v0.1.208` / `companion-v0.1.208`。网站部署 `6a9ef3ab9334038f43e7acc3` 已上线，正式域名 Series 13 个、Story 17 个脚本与构建逐字节一致，Story 包含更新后的裁切提示。

本机 `/Applications/AID Companion.app` 已安装 0.1.208，3018 状态与 CORS 正常，运行文件与打包构建匹配。两集完成、无活动导出时安装，四类持久化数据安装前后哈希完全一致。旧完整 0.1.207 备份于 `out/releases/v0.1.208/AID Companion-0.1.207-installed.app`。

两集裁切后分别为 105.791 秒 / 48,937,920 字节、117.859 秒 / 54,854,224 字节，原交付 ID 不变，交付接口 Range 已确认新文件。第二集回执 `outputs/halo-headtrim-20260908/episode2/trim-result.json`。

GitHub 三平台安装包 Actions `34147492125` 的三个构建及 release 均成功，公开发布 `companion-v0.1.208`。发布、下载和安装回执在 `out/releases/v0.1.208/`。
