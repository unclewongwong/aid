# 两条侯府视频质量诊断（2026-09-07）

用户确认继续使用 DaSiWa。本轮读取真实 ComfyUI history、原始视频抽帧和首帧，没有换模型或重跑视频。

| 任务 | 核验结果 |
| --- | --- |
| `4dbbac4a-5c16-4cfa-bcc0-8946d760a257` | 15 秒参考图生视频；原始输出出现对白字幕。首帧中玲玲、薛夫人、两名丫鬟已经面对面对峙，动作却要求玲玲从房中走出、丫鬟跟出、薛夫人再走近。 |
| `d16b54d2-2f0a-472d-bb59-fea664d9922b` | 15 秒参考图生视频；首帧已经是丫鬟张臂拦路，动作仍从夫人快步前进、丫鬟从侧面冲入开始。提示还同时要求踢向脚边地面与丫鬟顺冲力趔趄，缺少明确的接触因果。1 秒间隔抽帧未见对白字幕。 |

两条均为 `DasiwaMinimaxH3_dasiwaREF2VAHybridV1.safetensors`、专用 `minimax_h3_turbo_4step_dasiwa_ref2va_hybrid_v1_T8.safetensors` 强度 1、DualClock 四步、shift 12/3、simple。实际编码为 736×1280、24fps、H264 CRF19。无证据表明本轮偷偷切换其他模型或在导出阶段产生字幕。

两条仅送入一张首帧及两份音色，没有另送人物正脸参考，也没有 Motion Context。夫人在两张首帧中主要以背面出现，后续转头缺少直接的正脸图像约束。这个输入限制可能影响身份稳定性，尚未做加正脸参考的受控对照。

现有 H3_DIALOGUE_NO_SUBTITLE_POLICY 已实际进入请求，但第一条仍烧录字幕。因此此前两个成功样本的禁字幕提示不能推广为全面解决。字幕产生于模型原始输出；具体触发原因仍未知。

已确认的是首帧与动作起点矛盾、动作因果矛盾和原始输出字幕。它们是应优先处理的输入/输出问题；没有同图同种子对照，不能量化其对全部画质问题的贡献，也不能凭四步参数认定流畅性、面部纹理已合格。后续适合先仅修起点/动作因果，保留模型、种子、音色与时长做对照；这一实验尚未执行。

提示词沿用项目已有中文正文决策。本地 H3 skill 建议英文正文；本轮不把语言差异当成已证实画质根因，也未擅自改变该决策。

证据（本地、不提交原始用户内容）：`out/verification/recent-h3/two-task-history.json`、`media-info.json`、两个任务的 `*-reference.webp` 与 `*-frames.jpg`。
