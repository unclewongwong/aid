# AID 0.1.229

Qwen-Image-2.1 pure text-to-image now supplies the native ComfyUI `images` Autogrow container even when it is empty. Editing references use the same nested container contract.

This is a Companion and website hotfix for 0.1.228. It does not change the cloud model files, MiniMax H3 workflows, model pairing, LoRA, samplers or prompts.
