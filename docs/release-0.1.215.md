# AID 0.1.215

修复真实逐镜生成时的重复提交：上游任务明确失败后，旧界面清掉任务编号，修复中枢又可能根据尚未更新的页面状态误判为可续查，实际循环创建了替代任务。现在先识别invalid_image_file与终止失败，再考虑续查；失败任务保留原编号、模式和回执历史，单图失败同步保存状态，自动接续停止。只有用户直接操作的非审核失败重试允许创建替代任务，审核规则保持不变。

30组620项发布检查通过。包括终止失败优先于旧taskId、回执跨序列化保留与去重。安装和发布状态以out/releases/v0.1.215回执为准。

《为自己》已完成并查看修正后的1/3/5图，电脑/还款手机/遮屏动作均归正确人物。第9镜参考图片被上游判定为invalid_image_file；原潘达面膜JPEG本机下载与解码正常，尺寸5712×3213。已生成完全相同解码像素的PNG兼容副本用于受控验证，原文件保留；尚不能确认上游具体限制或宣称整集完成。
