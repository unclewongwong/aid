# AID 0.1.204

移除连续剧全局任务串行锁。独立项目、已完成共享素材的不同集、单项资产准备可并发；ComfyUI继续使用原生GPU队列，API任务不再被视频等待阻塞。

并发任务各自持有心跳、暂停信号、写入范围和快照版本，避免分集进度相互覆盖。同集防重复、共享素材依赖、上一段尾帧依赖与断点续查保留。Story独立API视频同时提交。

网站与Companion需同步更新。验证及边界见 [并发说明](series-api-concurrency.md)。发布回执保存在 `out/releases/v0.1.204/`。
