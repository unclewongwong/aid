# AID 0.1.216

为图像供应商保存独立的参考图传输副本。副本绑定原图URL和特定模型，只在该模型的图像请求中使用；原始资产、原图理解、其他模型与显式参考数组继续使用原图。原图被更换后旧副本自动失效，防止错用旧商品或把供应商兼容格式反写为资产本身。

修复《为自己》将同像素27MB PNG作为全局素材后，DMX原图理解报Image size exceeds the limit的问题。原图恢复为原JPEG，同尺寸同像素PNG只供gpt-image-2-official使用。文件过大错误现在归类到参考图片问题，停止自动文本修稿/重提。

继承215已修复的终止失败回执保留和自动循环阻止。回归与发布证据位于out/releases/v0.1.216；原图兼容实验尚待真实生图结果验证，不能声称整集完成。
